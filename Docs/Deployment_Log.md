# Deployment Log — Rotations Plus

Running record of every change that ships. One row per merge/deploy: date + time, PR number, target environment, and a one-sentence summary. Newest first. (Process: `CLAUDE.md §6`.)

| Date / Time (PT) | PR | Env | Summary |
|---|---|---|---|
| 2026-06-30 11:03 | [#89](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/89) | DEV | **Admin Programs list — production parity (port).** First of the admin-screen parity sweep (Programs → Rotations → Dashboard). Diffed the rewrite against the production source (`AdminProgram.js` + `FilterProgram.js`) + the owner's screenshots; the list already matched on tabs/search/card-columns/Retail-Amount(=honorarium), leaving two real gaps. **(1) Program Name is now a real field** — production renders `program_name` as a column distinct from specialty, which the rewrite was faking as `"{Specialty} Physician"`. Added `ProgramName` end-to-end: entity + EF config + **`AddProgramName` migration** (nullable `varchar(200)`) + Summary/Detail responses + Create/Update requests + projections + trim/length validation; the list shows the stored name, falling back to the legacy default only when null. **(2) FilterProgram modal now matches the live controls:** location → a **City/State dropdown** (InPerson/InPersonResearch tabs only) backed by a new **`GET /api/programs/locations`** distinct-locations endpoint (server pagination means the client can't derive it); Specialty/Approval Type/Clinical Needs → **checkbox lists with "All"** (19 tags in exact legacy order + the "100% Inpatient Hospitalist"→`Hospitalist` id remap); Retail Amount → a **dependency-free dual-range `RangeSlider`** (0–15,000) + min/max inputs; **per-tab filters** (legacy `programFilter0..4`); uppercase **YES/NO**. **Review:** 3 independent adversarial agents (backend/security **SHIP**, production-fidelity **SHIP**, frontend **BLOCK** → fixed the per-tab-filter/stale-city + RangeSlider converged-thumb + amount clamp + badge-count + casing → re-review **SHIP**, no further changes). **249 frontend tests + 72 API unit tests pass**; coverage 84%; `tsc -b` + prod build clean; migration preflight clean. Backend integration tests (ProgramName round-trip/validation, locations distinct/staff-readable, authz-matrix row) run in CI. |
| 2026-06-30 10:30 | [#88](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/88) | DEV | **Landing hero — mobile headline parity + review-debt cleanup.** Closes the one real finding from the **belated independent review** of PRs #86/#87 (which had been merged without review — owner flagged the process miss; the review-process memory + this row record the correction). Finding: the hero headline collapsed to a single variant, whereas production's `Hero.js` renders **two desktop lines** ("Find Your Perfect" / "Clinical Experience Today") and **three mobile lines** ("Find Your" / "Perfect **Clinical**" / "**Experience** Today"). Restored both variants via `.desktop-only-show`/`.mobile-only-show` utilities toggled at the port's app-wide 820px breakpoint (consistent with the hero's bg/font/padding swaps). Also cleaned stale "map" mentions in `styles.css` + `PublicCatalogEndpoints.cs` + `HeroSearch.tsx` comments. The other review finding — landing has no `<h1>` — **matches production (which also has none)**, so deferred as a post-cutover SEO enhancement, not changed. **Reviewed before merge** (adversarial agent → clean: toggle complete at every width, headline char-for-char vs production, tests resolve uniquely). 242 frontend tests pass; `tsc -b` + build clean. Process: from here every push runs the independent review→fix loop BEFORE merge — no exceptions. |
| 2026-06-28 20:40 | [#87](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/87) | DEV | **Landing hero rebuilt to match production.** The owner flagged the hero "not matching production at all." Investigation found production's `/` is `Home.js` → `<Hero/>` (a designed hero on a **full-width doctor illustration background**, `hero.webp`/`mobile_hero.webp`, with the headline + subtitle **left-aligned** at ~50% width and the search + 8 filters below) — whereas the rewrite had shipped a **flat centered pink box** with the **map always visible**. Rebuilt `HeroSearch` to clone production: copied the `hero.webp`/`mobile_hero.webp` assets; `.hero` now uses the background image, `min-height 800px`, `padding 280px 100px 0`, left-aligned PT-Serif 40px headline ("Clinical Experience" in brand `#FF4874`), PT-Sans subtitle at 50% width; production search-bar + the 8 filter dropdowns (Specialties/Program Type/Clinical Needs/Ratings/City/State/Duration/Pricing), dropdown options from the public catalog feed. **Per production, the map/results are NOT shown to an anonymous visitor** — running a search routes to the CIAM sign-in at `/portal` (production gates results behind login). Removed the Leaflet map entirely from the public landing → dropped `react-leaflet`/`react-leaflet-cluster`/`leaflet` deps, deleted the `usCityCoordinates` util, removed the OSM tile-server from the CSP (no external map calls); **bundle 1041 kB → 831 kB**. **Browser-verified the production build** (vite preview + Preview MCP eval): no console errors; hero bg image + headline + accent + subtitle + search + 8 filters; no Leaflet; left-aligned at the 100px padding — matches production. 242 frontend tests pass; `tsc -b` + build clean. Supersedes the hero from PR #85/#86. |
| 2026-06-28 19:55 | [#86](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/86) | DEV | **Hotfix — landing page crash (React error #130).** PR #85's hero shipped a white-screen crash on the public landing (`Unexpected Application Error`, React #130 "element type is object"). Root cause: **`react-leaflet-cluster@2` is CJS-only and, under the Vite 8 / rolldown production bundle, its default export resolves to the module *namespace object* rather than the component** — so `<MarkerClusterGroup>` rendered an object and crashed React. The jsdom tests mocked the leaflet libs, and the dev server bundles differently, so neither caught it — it only reproduces in the real production bundle. **Fix:** drop the cluster wrapper and render the Leaflet `<Marker>`s directly (map + markers + popups intact; **clustering deferred** to a build-compatible approach — follow-up). **Verified the production build in a real browser** (vite preview + Preview MCP): zero console errors, hero + search + 8 filter dropdowns + map + reviews + benefits + partners + footer all render. 248 frontend tests pass; `tsc -b` + build clean. **Process lesson:** components from CJS-only deps must be smoke-tested in the actual production bundle, not just jsdom-mocked unit tests. |
| 2026-06-28 19:25 | [#85](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/85) | DEV | **Public-site production-parity pass + mandate hardening.** Realigns the project on the production-parity Prime Directive (owner reset 2026-06-28: this is a *port* — UI/UX must match current production ≥99%, no missing features/no considerable UI changes; code improvements welcome; UX improvements deferred to post-cutover; production is ground truth, Figma the aid, production wins on conflict). **Guidance:** added the ⭐ Prime Directive to `CLAUDE.md` + reconciled §7; broadened the `match-production-ui-first` memory to the whole port (logged the public-site simplification as a 2nd incident); added a "Production parity is mandatory" section + per-page parity checklist to `Docs/Plan_Public_Site.md`. **Parity pass** (a fidelity audit vs `Live_Code` had the public site at ~88%): **Footer** — restored the 4 social icons (FB/IG/YouTube/Reddit), corrected address → *777 S Figueroa Ste 4600* and email → *info@rotationsplus.org* (same `.com→.org` fix in Terms/Privacy/FAQ copy), footer nav now lists **Blog** (live in prod) not Resources; **Resources** — restored the decorative content-type chips (Webinar active) + per-article *Learn more* + *Show more* paging; **Hero search** — activated Clinical Needs + Ratings, added **Sort** dropdown + **Reset filters** in production's order (data-less advanced controls login-gated → `/portal`); **Testimonials** — static Google-reviews grid → dependency-free rotating carousel (auto-advance, dots, arrows; no external embed); **For Preceptors / Consulting** — generic step template → the 5 alternating image-paired onboarding blocks (`onboard1-5`) + mobile image variants (valid `<picture><source>`), Consulting process CTA corrected to "Join Us Today". **Review:** 3 adversarial reviewers (frontend correctness / production-parity / a11y+CSS+tests) → fixed the footer Blog-vs-Resources divergence, the Consulting CTA, the carousel aria-live/dots semantics, and `<picture>` markup; re-review pass yielded no further changes. Owner-flagged deferrals (kept as-is): the legacy About route is a stub but we keep the fuller About page; the production "6th placeholder article" stays dropped. **248 frontend tests pass**, public coverage ≥90%; `tsc -b` + build clean. Continues the public-site program (`Plan_Public_Site.md`); resumes Admin Dashboard nodes next. |
| 2026-06-27 04:00 | [#84](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/84) | DEV | **About, Our Process, Our Team, FAQ, Resources, Legal (Public-Site program LP-3/4/5).** Completes the public marketing site (Blog deferred). **LP-3:** `OurProcessPage` (`/our-process`, 6-step journey verbatim), `OurTeamPage` (`/our-team`, About-Us intro + "Meet Our Team" 4 members + CTA), `AboutPage` (`/about` — the legacy About route was an empty stub, so a clean on-brand page: mission + Trust/Transparency/Technology values, **designed fresh**). **LP-4:** `FaqPage` (`/faq`, **8-category tabbed accordion** — Requirements/Expectations/LOR/Payment/Housing/Visa/Preceptors/Other, content verbatim; `FaqAccordion` extended to render multi-bullet answers, `role=tablist/tab/tabpanel`), `ResourcesPage` (`/resources`, 5 article cards — legacy placeholder 6th dropped). **LP-5 (legal — faithful reproduction of the live copy; flagged in-code for a final legal proofread before the public `www` cutover):** `TermsPage` (`/terms`, **full Terms of Service, 11 sections** via a reusable `LegalPage` renderer — TOC + numbered sections + lettered clauses), `PrivacyPolicyPage` (`/privacy-policy` — mirrors the live page, which only contains the intro + 14-item contents list + section 1; the rest of the body is absent in the source, flagged for completion). All CTAs → CIAM sign-up at `/portal`. **Review:** 2 adversarial reviewers (legal-fidelity + React/CSS/regression) — both **SHIP**; legal text verified **clause-by-clause** (every dollar amount, deadline, obligation exact; a few legacy artifacts cleaned), `FaqAccordion` union widening confirmed backward-compatible with LP-2, zero `dangerouslySetInnerHTML`. Fixed pre-merge: FAQ tab→panel ARIA wiring. **Owner-flagged for www cutover (legacy content, not code bugs):** FAQ says the unlock fee is **$25** while Terms say **$99** (contradiction exists on the live site); Privacy Policy needs full legal copy; the Housing FAQ's RotatingRoom affiliate link is rendered as plain text. **Owner decision 2026-06-27: ship legal as-is on the (private) DEV URL, revisit before public go-live.** Tests: every page (content + CTA-→/portal + FAQ tab-switch + multi-bullet expand + legal TOC/sections). **229 frontend tests pass**, public coverage 98%; `tsc -b` + build clean. Merged after LP-2 (rebased onto it). **LP-3/4/5 of the public-site program; the public marketing site is now complete except Blog.** |
| 2026-06-27 03:15 | [#83](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/83) | DEV | **For Preceptors + Consulting Services pages (Public-Site program LP-2).** Cloned the legacy `ForPreceptors` + `ForConsultants` marketing pages (copy verbatim) onto **`/for-preceptors`** and **`/consulting-services`**, replacing the LP-1 placeholders. Shared **`AudienceFunnelPage`** (hero → About → Benefits → banner → 5-step Process → FAQ) since the two pages are structurally identical and differ only in content; reusable **`FaqAccordion`** (single-open; also feeds the FAQ page in LP-4). The legacy "process" steps used **old-app screenshots** → rendered as clean numbered step cards instead so the public site never shows stale admin UI (generic hero/about/banner photos cloned from `Live_Code`). **All CTAs route into the customer (CIAM) sign-up at `/portal`.** **Review:** 1 adversarial reviewer (low-risk static pages — no auth/backend) — **SHIP**; confirmed content fidelity (legacy typos "Perfomance"/"Preperation" fixed), the CTA→`/portal` invariant, a11y (button `aria-expanded`, decorative `alt=""`, clean heading order), and the all-links-→/portal test assertion is valid. Fixed pre-merge: funnel step-number color was `--brand-tint` (near-white on white card → invisible) → `--brand`. Tests: `FaqAccordion` (open/exclusive/close), `ForPreceptors` (content + every CTA → `/portal` + FAQ expand), `ConsultingServices`. **221 frontend tests pass**, public coverage 97%; `tsc -b` + build clean. **LP-2 of the public-site program (`Plan_Public_Site.md`).** |
| 2026-06-27 02:30 | [#82](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/82) | DEV | **Public landing page + routing/auth restructure (Public-Site program LP-1).** First piece of the public marketing site — the rewrite had no public front door (the root `/` force-redirected into staff login). Now **three independent router branches**: (1) **public anonymous `/`** (`PublicLayout`, **no MsalProvider**) with a faithful clone of the live `www.rotationsplus.org` **Landing page** (hero + Search-Programs CTA, Our Benefits, How-It-Works, Testimonials, dual CTA, Our Partners) + the other marketing routes wired to a `PublicComingSoon` placeholder (built out in LP-2…LP-5; Blog deferred); (2) **staff workforce-MSAL** branch (`StaffMsalShell` → `Outlet`) with the legacy login URLs **`/rotationsplusadmin` · `/rotationsplussales` · `/rotationsplussdr`** (thin `StaffLoginLauncher`s → `loginRedirect`, then role-route via `roleHome()`) + the `/admin/*` console; (3) **CIAM portal `/portal/*`** unchanged. **Auth change:** workforce `redirectUri` moved from `/` (origin) to **`/admin`** so the anonymous root never processes an MSAL hash and the two MSAL instances never contend (no-root-provider invariant preserved); `postLogoutRedirectUri` → public `/`. Landing CTAs send students/preceptors into the existing rplus-susi **CIAM sign-up** at `/portal`. Shared `Footer` extracted from `AppLayout` (reused by admin/portal/public); marketing images cloned from `Live_Code`. **Owner action (Entra, this deploy):** register the per-env `/admin` SPA redirect URI on `rplus-web` and **keep the origin `/` URI permanently** (it's now the sign-out target). **Review:** 3 independent adversarial reviewers (security/auth-routing, React/tests/a11y, regressions/fidelity) — all **SHIP**; the auth reviewer confirmed no anonymous bypass + genuine MSAL-instance isolation. Fixed before merge: launcher fired an unauth `/api/me` (moved `useMe` into the authed subtree), no retry on `loginRedirect` reject (added Try-again), already-signed-in user routed by entry not role (now waits for `useMe` → routes by `roleHome`); partner-link a11y name + mobile-menu toggle test. Tests: `roleHome` table, launcher (redirect + role-route), `PostLoginRedirect`, `PublicLayout` (provider-free proof + menu), `LandingPage`, `Footer`, `PublicComingSoon`. **217 frontend tests pass**, coverage 83%/76%; `tsc -b` + production build clean. **Known limitation:** non-admin staff w/o a built console (SDR/Coordinator) land on the AdminOnly dashboard (403, no leak — server-enforced; sign-out works); proper scoped landings come with the follow-on Admin Dashboard nodes. **LP-1 of the public-site program (`Plan_Public_Site.md`).** |
| 2026-06-27 01:30 | [#80](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/80) | main | **Release promotion — `develop` → `main` (owner-requested).** Promoted the full accumulated `develop` line (20 commits, PRs through #79 — Honorarium payout + Evaluation tab, Programs/Rotations production-parity, the two admin Filter modals, dashboard ToDo/Revenue/Reports/Campaign tabs, the document-management subsystem, server-side pagination, and the FIX-1/FIX-2 guards) onto `main` via merge-commit PR #80 (`92fdb94`, no-fast-forward to preserve release history). No rebuild/redeploy — promotion only. **Both branches mirrored to GitHub** (`karolykiraly/Rotationsplus`): `main` `92fdb94`, `develop` `9de3cd2`. (Production cutover infra unchanged — stealth rules still in force.) |
| 2026-06-27 01:00 | [#79](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/79) | DEV | **Admin Filter modals wired (UI-Parity program PR-4b + PR-3b).** The two deferred legacy filter modals are built and applied server-side over the paginated lists. **Programs (`FilterProgramModal`):** program number · location · specialty · instant-approval (Both/Yes/No) · honorarium range · tags — new optional `GET /api/programs` params (`specialtyId/city/instantApproval/honorariumMin/Max/programNumber/tags[]`). **Security:** the **honorarium filter is staff-gated** (`includeHonorarium`) so a customer (the endpoint is MarketplaceViewer) can't probe `honorariumMin/Max` to infer the preceptor-pay/margin the response already nulls; integration test proves a customer's honorarium filter is a no-op. Tags use Postgres `text[]` overlap, capped at `MaxTags`. **Rotations (`FilterRotationModal`):** start/end date · amount range · needs-visa · status · rotation number — new optional `GET /api/rotations` params (`startFrom/endTo/retailMin/Max/needsVisa/rotationNumber`); **one shared filter narrows BOTH Current/Historical sections** (legacy parity). Both modals have a filter button with an active-count badge; the legacy dual-range sliders are rendered as min/max inputs, and Programs' location is a free-text "City, State" (contains-match). No migration. **Review:** full adversarial roster (security/backend-EF + frontend/tests) + a final verification pass — **all CLEAN**; the security reviewer confirmed no customer-probe leak and the Npgsql array-overlap/correlated-subquery translations are safe (CI runs the integration tests). Tests: backend filter-isolation per dimension incl. the customer no-probe guard; component apply/clear + count for both modals. 202 frontend tests pass; `tsc -b` + solution build clean. **Completes Programs + Rotations parity.** |
| 2026-06-27 00:30 | [#78](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/78) | DEV | **Rotations screen matched to PRODUCTION (UI-Parity program PR-3).** `/admin/rotations` reworked to the live `AdminRotations.js` layout: **two stacked sections** — *Current Rotations* + *Historical Rotations* (each server-paginated with its own search) — replacing the single list + status-filter dropdown. Production columns **Rotation # · Preceptor · Student · Start–End · Retail Amount · Needs Visa ☑ · Status** (colour-coded, `NotStarted`→"Approved"); dropped the rewrite's Specialty/Type/Weeks columns and the **inline Edit/Delete/Refund** (owner decision 2026-06-26 — hide to match production; the backend DELETE/refund endpoints remain). A **View** button opens the **Selected Rotation** detail panel (Replace program / Change dates / status dropdown / Save), saved via the single audited update endpoint (state-machine + money-overflow guards still apply, incl. on a Replace). **Backend:** `RotationSummaryResponse` +`RetailAmount` (retail/wk × weeks) +`NeedsVisa` (correlated EXISTS on the booked student's `VisaStatus == NeedsVisaHelp`); a **`scope=current\|historical`** list param (Current = non-terminal Pending/NotStarted/Active/ToBeEvaluated, Historical = terminal); `RotationDetailResponse` +`ProgramNumber` +`RetailAmount` (Rotation Cost) +`PaidAmount` (sum of Succeeded payments). No migration. **Review:** full adversarial roster (backend/EF, data/money, frontend/parity/tests) — **all CLEAN**; the EF correlated-subquery/array patterns verified against existing shipped code. Tests: backend (retail+visa, scope split, detail fields, paid-amount reflects a captured deposit) + frontend rewrite (two sections, columns, View→panel, Replace/Change/status, Save). **CI hiccup:** first build rejected by `tsc -b` (noUnusedLocals) on an unused import that local `tsc --noEmit` missed → fixed + re-pushed green (recorded as memory `ci-typecheck-tsc-b`). **PR-3 of the Production UI/UX Parity program.** |
| 2026-06-26 20:05 | [#77](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/77) | DEV | **Programs list columns matched to PRODUCTION (UI-Parity program PR-4).** The `/admin/programs` list now mirrors the live `AdminProgram.js` columns: the **Program Name** column derives **`"{Specialty} Physician"`** (the legacy default — the rewrite has no standalone program-name field) so it's no longer a duplicate of the **Specialty** column (which keeps the bare specialty); the **"Retail Amount"** column now shows the **weekly honorarium** (owner decision 2026-06-26 — production displays `weekly_honoarium` under that label even though a separate retail field exists), with a `—` fallback. **Backend:** `ProgramSummaryResponse` gains a **staff-only nullable `WeeklyHonorarium`**; both `GET /api/programs` and `/catalog` (MarketplaceViewer — students can call them) **null it for customer callers** after materialization, mirroring the detail endpoint, so the preceptor-pay / platform-margin stays hidden. No migration (the column already existed on the entity). **Frontend:** `Program` type +`weeklyHonorarium`; `programDisplayName` helper; the list renders the derived name + honorarium. **Review:** full adversarial roster (**security/authz**, backend/data/money, frontend-parity/tests) — **all CLEAN**; the security reviewer verified the customer redaction is airtight on both endpoints. Tests: integration asserts staff see the list honorarium and customers get it stripped on both `/api/programs` + `/catalog`; component asserts the derived name, distinct specialty, honorarium-under-Retail-Amount, and the `—` fallback. 208 frontend tests pass; solution builds clean. **Filter modal deferred to PR-4b.** **PR-4 of the Production UI/UX Parity program.** |
| 2026-06-26 19:15 | [#76](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/76) | DEV | **Honorarium Evaluation tab matched to PRODUCTION (UI-Parity program PR-2).** The `/admin/honorarium` **Evaluation** tab now renders the live `AdminHonorarium.js` tab-2 columns exactly: **Rotation Number · Preceptor Name · Student Name · Honorarium Amount · Evaluation Upload Status · Evaluation Due Date · Payment Status** — dropping the **Rotation Start Date** column the Deposit/Start tabs keep. **Upload Status** renders the literal **"Completed"** (legacy hardcodes it; the real evaluation-document subsystem is a later phase). **Backend:** `Honorarium` gains a nullable **`EvaluationDueDate`** (migration `AddHonorariumEvaluationDueDate`, `has-pending-model-changes` clean) — snapshotted at generation in `HonorariumGenerator` as **rotation `EndDate` + 7 days** (the legacy `due_date = end_date + 7d`); nullable so rows generated before the column existed render **"-"** (matching the legacy fallback). Surfaced through `HonorariumResponse` + the list `Summary`/`ToResponse` projections (scalar-only, no client-eval). The money split, cent-exact allocation, and the product-overflow guard are **untouched**. **Frontend:** `Honorarium` type +`evaluationDueDate`; `HonorariumPage` swaps the start-date cell for the two evaluation cells on the Evaluation tab only (`last-td` logic preserved across all three tabs). **Review:** full adversarial roster (backend correctness, data/money/concurrency, frontend-parity/tests) — **all CLEAN, no code changes**; verified column-for-column against the legacy reference. Tests: integration asserts the due-date snapshot (`2026-10-05 + 7 = 2026-10-12`); frontend +2 (Evaluation columns render incl. literal "Completed" and the formatted due date, **and Rotation Start Date is absent**; null-due-date → "-"). 207 frontend tests pass; coverage 84%/77%; solution builds clean. **PR-2 of the Production UI/UX Parity program.** |
| 2026-06-26 18:30 | [#75](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/75) | DEV | **Permission screen reworked to PRODUCTION (UI-Parity program PR-1).** Course-correction: PERM-1 (PR#73) had deviated from the live screen (per-row Approve button + Reject-with-reason modal; columns Name/Email/Specialty/Location) — the mandate (CLAUDE.md §7; owner 2026-06-19, reaffirmed 2026-06-26) is **match production first, improve after Production cutover**. The screen now matches the live `/admin/permission` exactly: header "Permissions" + "N items"; columns **Preceptor Name · Specialty · Scheduled (Yes/No, green/pink) · Phone Number · Email · Activated ☑ · Reject ☑**; a single **Save** (batch). **Backend:** `Preceptor` gains **`MobilePhone`** + **`CallScheduled`** (migration `AddPreceptorPhoneAndScheduled`, `has-pending-model-changes` clean; both display-only — the call-booking flow is post-cutover; a seeded Pending preceptor + phone/scheduled values added for DEV); the queue/list/options projection now returns them. The PERM-1 per-row `/approve` + `/reject` endpoints (and `RejectPreceptorRequest`) were **removed** and replaced by **`POST /api/preceptors/permissions { activateIds[], rejectIds[] }`** (AdminOnly) — applies the whole batch in one transaction (Pending-only; activate→MemberActivated, reject→Rejected **reason-less** matching production's binary checkbox; an id in **both** lists → 400; stamps reviewer oid + time). **Frontend:** `Preceptor` type +`mobilePhone`/`callScheduled`; `savePreceptorPermissions` replaces `approve/rejectPreceptor`; `usePermissionQueue` (Pending list + batch save, invalidates preceptors/options/dashboard-todos); `PermissionPage` rebuilt to the production columns + checkboxes + Save (Activated/Reject **mutually exclusive** per row so a row can't be sent as both → avoids the 400). **Review:** full six-reviewer roster (identity/authz risk area) + adversarial loop. Tests: backend batch activate+reject+stamp, only-affects-Pending, id-in-both 400, queue-returns-phone+scheduled, status-filter + the `/permissions` authz-matrix row (replacing the 2 approve/reject rows); frontend 8 (production columns render, non-admin block, activate-on-save, reject-on-save, mutual-exclusivity, empty-save warning, error banner, empty state). 205 frontend tests pass; solution builds clean (Release, 0 warnings). **Recorded the corrected principle as memory `match-production-ui-first`; this is PR-1 of the Production UI/UX Parity program (rework all built admin screens + Student/Preceptor forms).** |
| 2026-06-26 16:45 | [#74](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/74) | DEV | **Preceptor honorarium (payout) screen — admin `/admin/honorarium` (HON-1; completes the preceptor economic loop alongside the PERM-1 approval queue).** The legacy 3-stage payout screen, reimplemented. New **`Honorarium`** entity (`payments` schema) — one row per **stage** (Deposit / Start / Evaluation) per rotation, with snapshot `PreceptorName`/`StudentName`/`RotationNumber`/`RotationStartDate` (self-contained display, no joins), `Amount`/`Currency`, `Status` (Pending/Paid/Cancelled, string-stored), independent `Refunded` flag, and `PaidBy`/`PaidAtUtc` audit. Migration **`AddHonorariums`** (`has-pending-model-changes` clean) with a **unique `(RotationId, Stage)` live-row index**. **Generation:** `HonorariumGenerator.EnsureForRotationAsync` is called from `PaymentFulfillment` on **deposit success** (the legacy paid-booking trigger) — total = program **weekly honorarium × weeks**, split **25/25/50**, allocated **exact-to-the-cent** (last stage absorbs rounding drift); **idempotent** (existence check + the unique index backstop), so a re-delivered success never double-schedules. **Endpoints (all AdminOnly):** `GET /api/honorariums` (stage/status/`q`-search paged list, deterministic `RotationStartDate, Id` order), `POST /{id}/pay` (Pending-only → 409; **server-gated in-order** — Start needs Deposit paid, Evaluation needs Start paid; stamps PaidBy/PaidAtUtc), `POST /{id}/refund-flag` (toggles the bookkeeping flag). Plus **`DELETE /api/honorariums/{id}`** (AdminOnly soft-delete of an erroneously-generated row; **refused 409 if Paid**). Frontend: `Honorarium` types + `getHonorariums`/`payHonorarium`/`setHonorariumRefund`/`deleteHonorarium`; **`useHonorariums`** (per-stage paged list + pay/refund/delete, invalidates the whole `["honorariums"]` key since paying one stage unlocks the next); **`HonorariumPage`** (3 stage **tabs**, server-paginated, keepPreviousData + "Updating…"/aria-busy + fresh-data clamp); wired the **"Honorarium" sidebar item** to the route (was "coming soon"). **UI matched to owner-supplied Production screenshot (2026-06-26):** exact column labels (Rotation Number / Preceptor Name / Student Name / Honorarium Amount / Rotation Start Date / Refunded / Payment Status), "N items" count header, `$450`-style amounts (no forced cents), **outline Pay button** (new `.btn-outline`), **Paid badge**, refunded checkbox + **guarded Delete button** (confirm modal) on the Deposit tab. **Owner decisions this turn:** wire a real guarded Delete (vs production's no-op button); **realign the Permission screen to Production in a separate follow-up PR** (columns Specialty/Scheduled/Phone/Email + Activated/Reject checkboxes + Save — needs preceptor phone + a "scheduled" concept). Evaluation-tab upload-status/due-date columns deferred (evaluation-document dependency). **Notes/deferred (logged):** marking paid is **bookkeeping only** (no gateway disbursement — preceptor payouts are external today); generation hooks the **deposit-success path only** (a manual admin Pending→NotStarted without a deposit doesn't generate a schedule — revisit if that becomes a real flow); preceptor `total_honorarium` roll-up is **derivable** from rows (not denormalized → no increment race). **Review:** full six-reviewer roster (money path: backend/correctness, security/authz, data/concurrency/money, performance, frontend, tests/DoD) + adversarial verification — **3 SHIP, 3 BLOCK**, all blockers verified against the code and fixed: **(HIGH, data)** `total = WeeklyHonorarium × Weeks` could overflow `numeric(10,2)` → overflow inside the deposit-fulfilment txn → webhook 500-loop wedging a paid student. Fixed in two layers after a re-review caught that a weeks cap alone is insufficient (the *product* of two independently-bounded factors overflows — e.g. max weekly × 2 weeks): a **520-week sanity cap** on the admin `TryValidateDates` AND a **product guard `TryValidateMoney`** (rejects a booking whose retail×weeks or weekly-honorarium×weeks exceeds the `numeric(10,2)` ceiling, on admin create+update, 400 before any money moves), PLUS a **defense-in-depth guard in `HonorariumGenerator`** (skips generation rather than throw if a total would overflow, so no creation path can wedge the webhook). Tests: weeks-cap 400 + product-overflow 400; **(HIGH, perf)** the list `WHERE Stage=… ORDER BY RotationStartDate, Id` had no supporting index (only the `(RotationId,Stage)` unique one) → full scan + top-N sort, and honorariums already show 600+ rows/stage-tab in prod → added **`IX_honorariums_(Stage, RotationStartDate, Id) WHERE IsDeleted=false`** (folded into the AddHonorariums migration); **(HIGH, tests)** the cent-remainder branch (`total − allocated`) was untested because the seed total 2000 divides cleanly → added a **10.02-over-1-week** test pinning 2.51/2.51/**5.00** (remainder, not naive 5.01) + a zero-honorarium-skips test + a **soft-deleted-stage-isn't-regenerated** test (reset payment→Pending, re-simulate, assert live count stays 2) + a status-filter test. Lower findings deferred with reason: the unique-index 23505 self-heals on webhook retry (extremely narrow, fulfilment is payment-status-guarded); `pay`/`delete` last-write-wins is benign bookkeeping (no gateway money movement) — both revisit if pay ever triggers real disbursement; the **customer self-booking path** doesn't run `TryValidateMoney`, so an over-product deposit `TotalAmount` could overflow — but that throws at **intent-creation** (a recoverable 5xx to that caller, no money moved, NOT a webhook wedge) and is pre-existing, so it's a tracked follow-up (apply the same product guard on the customer path), not a blocker here. Tests: backend generation (3 stages 500/500/1000 + exact sum + snapshots), **cent-remainder exact**, zero-rate-skip, no-duplicate-on-resettle, **soft-delete-no-regenerate**, stage + status filters, oversized-q 400, pay-deposit, out-of-order-pay 409 (×2), in-order pay, already-paid 409, unknown 404, refund toggle + unknown 404, delete pending/paid-409/unknown-404 + the admin weeks-cap 400 + 4 authz-matrix rows; frontend 11 (stage request, non-admin block, tab switch, pay+banner, out-of-order error banner, refund toggle, Paid badge, delete-confirm, paid-delete-409 banner, no-Delete-outside-Deposit, empty state). 204 frontend tests pass (honorarium 92%); solution builds clean (Release, 0 warnings); migration `has-pending-model-changes` clean. **Lights up the "Honorarium" nav; with PERM-1 the preceptor supply side now has both approval and payout.** |
| 2026-06-26 14:30 | [#73](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/73) | DEV | **Preceptor approval queue — admin Permissions screen (PERM-1; first net-new admin feature beyond the dashboard/marketplace surface).** The dashboard ToDo's "preceptor approvals" count had nowhere to go; this builds the destination — `/admin/permission`. First slice of the legacy permission screen: **approve/reject with audit**. `PreceptorStatus` gains **`Rejected`** (string-stored → migration-safe append); `Preceptor` gains **`ReviewedBy`/`ReviewedAtUtc`/`RejectionReason`** (migration `AddPreceptorApprovalFields`, `has-pending-model-changes` clean). **`POST /api/preceptors/{id}/approve`** → MemberActivated; **`POST .../reject`** (required reason, ≤1000) → Rejected — both **AdminOnly**, both guard `Status == Pending` (else 409; the edit-form dropdown stays the manual override), stamp reviewer (`ICurrentUser.ObjectId`) + time (`TimeProvider`). `GET /api/preceptors` gains a **`status` filter** (the queue calls `?status=Pending`). Frontend: `getPreceptors` +status; `approvePreceptor`/`rejectPreceptor`; **`usePermissionQueue`** (Pending list + approve/reject, invalidates the preceptor lists + **picker options** + **dashboard-todos** count, seeds the detail cache); **`PermissionPage`** server-paginated queue (keepPreviousData, "Updating…"/aria-busy, fresh-data-gated clamp) with Approve + Reject-with-required-reason modal; wired the **"Permissions" sidebar item** to the route (was "coming soon") + repointed the dashboard ToDo link here. **Full six-reviewer roster** (`Docs/Review_Process.md` — risk area: state transition + authz + audit + identity) + adversarial verification → **no Critical/High**; fixes applied: symmetric `Reject_a_non_pending` 409 + oversized-reason 400 tests, cache-parity (invalidate `["preceptor-options"]` + seed `["preceptor", id]`), clear the inline error on typing, + a clamp-back frontend test. **Deferred with logged reason:** the agreement-PDF flow (§3.15a), W9, doc-due-dates, per-program access (deeper slices needing QuestPDF + email + the preceptor Documents page that isn't built yet); the approve/reject **last-write-wins race accepted** for this slice (no side-effects, no torn state, both terminal states valid — revisit with an atomic `ExecuteUpdate` claim when approve gains account-provisioning side-effects); no `Status` index (parity with the deferred index levers, the Pending set is small); approve/reject **emit no notification email** (fake sender until cutover — wire at the agreement/notification slice). Tests: backend approve/reject/conflict(×2)/oversized-400/404/status-filter + 2 authz-matrix rows; frontend queue render, approve, reject-requires-reason, error banner, empty state, clamp-back. 193 frontend tests pass; solution builds clean. **Lights up the "Permissions" nav + gives the preceptor-approval ToDo a working destination.** |
| 2026-06-25 22:40 | [#72](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/72) | DEV | **Server-side pagination — admin campaign list (PERF-1 slice 5; completes PERF-1).** The dashboard Campaign tab fetched every campaign; the list grows with each send. Paginate it — the simplest slice (AdminOnly, **no picker / no customer consumer**, no money). `GET /api/campaigns` now takes `page`/`pageSize` and returns `PagedResponse`; **newest-first with an `Id` tie-break** (time-ordered v7 GUID) so paging is deterministic when two campaigns share a created-at instant. No search (it's a recency list with no search box), no `/options`, authz unchanged (AdminOnly — existing matrix row still valid). Frontend: `getCampaigns`→`PagedResponse`; `DashboardCampaignPanel` server-driven (page state + `Pagination` + `keepPreviousData` + **"Updating…"/aria-busy** + fresh-data-gated clamp); composing a draft **jumps to page 1** so the new draft is visible; create/send invalidate `["campaigns"]` (prefix-matches all pages). **Full six-reviewer roster** (`Docs/Review_Process.md`: backend/security/data/perf/frontend/tests, independent + the standing rigor) → **all clean, no Critical/High/Medium**, no code changes from review. **Deferred with logged reason:** no `(CreatedAtUtc, Id)` index — acceptable at campaign scale (admin-authored, dozens–hundreds lifetime) and the established project pattern for recency-ordered lists, recorded as the scale lever; the identical-timestamp tie-break test (needs a settable `TimeProvider` the campaign test factory lacks — consistent with every prior slice) and a clamp-back test (the panel has **no row-removal UI**, so the clamp is defensive against external shrinkage only — not reachable to test meaningfully) were both deferred. Tests: backend list read → `PagedResponse` + a **disjoint-pages / newest-first** test (3 fresh drafts, page1/page2 pageSize=2, no overlap, CreatedAtUtc descending), normalization covered at the shared `PaginationExtensions` level; frontend paged envelope + **Next→page 2** (asserts the `page:2` request AND the page-2 row renders). 186 frontend tests pass; solution builds clean. **PERF-1 COMPLETE** — all five growable admin lists (rotations, students, preceptors, programs, campaigns) are now server-paginated; form pickers read dedicated unpaginated `/options` (students/preceptors) or `/catalog` (programs) endpoints; the customer-marketplace server-pagination redesign is the one explicitly-deferred outward-facing follow-up. |
| 2026-06-25 21:40 | [#71](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/71) | DEV | **Server-side pagination — programs admin list + catalog split** (PERF-1 slice 4). The programs list endpoint is shared three ways — admin list, rotation-form **program picker**, and the **customer marketplace browse** — so it couldn't be paginated in place. **Owner decision (this slice): "admin list + picker only"** — paginate the admin list, give the customer browse its own catalog path with behavior untouched, and **defer the customer-marketplace server-pagination+sort redesign** (its client-side price-sort + "Show more" reveal) to its own outward-facing slice. **Split:** `GET /api/programs` is now a **paged** admin list (`PagedResponse`) — program-type **tabs** (multi-valued `ProgramType[]`; the Consultation tab spans Consultation + ConsultationSub) + name search over **specialty/preceptor**, deterministic order (Specialty.Name, ProgramType, **ProgramNumber** unique tie-break), image read-URLs signed on the page; new **`GET /api/programs/catalog`** returns the **full filtered list** (specialtyId/preceptorId/programType/maxRetailPerWeek + q over specialty/**description**) — the prior marketplace logic — for the customer browse + the form picker. Both share a static `Expression` projection so EF JOINs the Specialty/Preceptor navigations ([[ef-static-method-projection-client-evals]]). Frontend: `getPrograms`→`PagedResponse` + new `getProgramCatalog`; `ProgramsPage` server-driven (type tabs + debounced search + `keepPreviousData` + **"Updating…"/aria-busy** + fresh-data-gated clamp); the rotation picker and customer `browsePrograms` both read `/catalog`; program CRUD **invalidates both** the paged list and the catalog. **Full six-reviewer roster** (`Docs/Review_Process.md`: backend/security/data/perf/frontend/tests, independent + adversarial verification) → **no Critical/High**; fixes applied: **unified the catalog search to the shared `TryBuildSearchPattern`** (removed a duplicated inline ILIKE escaper + a redundant `MaxSearchLength` const flagged for drift), **repointed the catalog `q`-too-long 400 test** (it had silently moved to the paged path, leaving the catalog guard uncovered), and added a **paged wildcard-literal** test + a **paged image-URL-signed** assertion (tightened both image checks to pin the rewrite, not just non-empty). **Deferred with logged reason:** no `(SpecialtyName, ProgramType)` btree / `pg_trgm` index — parity with the accepted student/preceptor slices, ILIKE `%term%` is non-sargable (an index would only aid the sort), directory is hundreds of rows; `/catalog` is deliberately unbounded (same fetch-all posture the browse already had — no regression), typeahead/keyset noted as scale levers. Seeding a `ConsultationSub` row to test the real Consultation-tab union was deferred (would ripple through the `TotalCount==4` assertions; the OR mechanism is proven generically + the exact tab payload is proven on the frontend). 185 frontend tests pass; solution builds clean. **Next slice:** campaigns. |
| 2026-06-25 19:50 | [#70](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/70) | DEV | **Server-side pagination — preceptors + picker options endpoint** (PERF-1 slice 3; applies the slice-1 foundation to the preceptor directory). `GET /api/preceptors` now takes `page`/`pageSize`/`q` and returns `PagedResponse`; server ILIKE search over **name, email, and location** (city/state, null-guarded) replaces the client-side search, with a **deterministic order** (LastName, FirstName, Id → total order so paging can't overlap/skip). New **`GET /api/preceptors/options`** — an **unpaginated** picker list (same StaffOnly DTO, no new data/audience) for the **program form's preceptor dropdown**, which needs every option, not a page (deliberately unbounded — fine at directory scale, → server-side typeahead later; authz-matrix row added). List + options share a **static `Expression<Func<>>` projection** so EF composes the `PrimarySpecialty.Name` navigation into the SQL as a **JOIN**. Frontend: `getPreceptors` → `PagedResponse` + new `getPreceptorOptions`; `PreceptorsPage` is server-driven (debounced search, `keepPreviousData`, **"Updating…"/aria-busy**, fresh-data-gated page-clamp) **mirroring StudentsPage**; the program picker (`useProgramFormOptions`) now reads the options endpoint (key `["preceptor-options"]`), and preceptor CRUD **invalidates both** the list and the options so the picker stays fresh. **Full six-reviewer roster** (`Docs/Review_Process.md`: backend/correctness, security/authz, data/concurrency, performance, frontend, tests/DoD — independent parallel agents + an adversarial verification pass) → caught **one Critical, fixed + re-verified**: the first cut shared the projection via a **compiled static method** (`Select(p => ToSummary(p))`), which EF **client-evaluates over un-joined entities** → `PrimarySpecialty` null → NRE/500 (the student slice's `ToSummary` only works because it's scalar-only); switched to an **`Expression` field** that EF composes into the query (JOIN generated) — CI's `PrimarySpecialtyName` assertion exercises the real SQL path ([[ef-static-method-projection-client-evals]] saved to memory). Security + data passes clean. Two reviewer-driven test additions (both verified non-hollow): a **location (city/state) search** integration test — the placeholder advertises it and only name/email were covered — and a **page-clamp-back** frontend test (delete the last row on page 2 → refetch reports one page → clamp to page 1; mutation-proven to fail if the clamp is removed). **Discarded with reason:** the "soft-deleted specialty orphans a preceptor" Medium is unreachable — FIX-1 (#66) already 409s a specialty delete while a live preceptor references it; the "seed city/state swap" was a false positive (`Seed` param order is `(state, city)`; call sites pass `IL/Chicago` correctly; CI asserts `City==Chicago`). **Deferred with logged reason:** no `(LastName, FirstName)` btree index — parity with the accepted student slice, the ILIKE `%term%` is non-sargable so an index only aids the sort, and the directory is hundreds of rows; recorded as a scale lever (partial `(LastName,FirstName) WHERE NOT IsDeleted` + `pg_trgm` GIN for search) alongside the documented typeahead path for `/options`. Tests: backend disjoint-pages via a unique-token cohort, search by name/email/**location**, options-includes-created, customer-cannot-list-options, over-length 400, list reads switched to `PagedResponse`; frontend server-side debounced search + Next→page 2 + **clamp-back**, program picker mock repointed. 185 frontend tests pass; solution builds clean. **Next slices:** programs, campaigns. |
| 2026-06-25 16:50 | [#69](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/69) | DEV | **Server-side pagination — students + picker options endpoint** (PERF-1 slice 2; applies the slice-1 foundation to the student directory). `GET /api/students` now takes `page`/`pageSize`/`q` and returns `PagedResponse`; server ILIKE search over **name, email, and location** (city/state, null-guarded) replaces the client-side search, with a **deterministic order** (LastName, FirstName, Id → total order so paging can't overlap/skip). New **`GET /api/students/options`** — an **unpaginated** picker list (same StaffOnly DTO, no new data/audience) for the **rotation form's student dropdown**, which needs every option, not a page (deliberately unbounded — fine at directory scale, → server-side typeahead later; authz-matrix row added). Frontend: `getStudents` → `PagedResponse` + new `getStudentOptions`; `StudentsPage` is server-driven (debounced search, `keepPreviousData`, **"Updating…"/aria-busy**, fresh-data-gated page-clamp) **mirroring the reviewed RotationsPage**; the rotation picker (`useRotationStudents`) now reads the options endpoint (key `["student-options"]`), and student CRUD **invalidates both** the list and the options so the picker stays fresh. **Full roster review** (backend/security/perf + frontend/tests) → **ship, no blocking findings** (PII/authz at parity with the existing list; nullable-column ILIKE is the proven `ProgramEndpoints` pattern, executed server-side in CI; dual-invalidation complete — only `useStudents` mutates students and only the picker reads the options key). Tests: backend disjoint-pages via a unique-token cohort, search by name/email, options-includes-seeded, customer-cannot-list-options, over-length 400, narrowed "contains seeded" (q=Rivera); frontend server-side debounced search, Next→page 2 + reset on status, empty state; RotationsPage picker mock repointed. 182 frontend tests pass; solution builds clean. **Next slices:** preceptors, programs, campaigns. |
| 2026-06-25 15:40 | [#68](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/68) | DEV | **Server-side pagination — rotations + shared paging foundation** (PERF-1 slice 1; first of a per-entity series replacing fetch-all-then-client-slice on the growable admin lists). **Foundation:** `PagedResponse<T>` (`Contracts/Common`) — `items` + `page`/`pageSize`/`totalCount` (+ derived `totalPages`), mirrored as a TS interface; **`PaginationExtensions`** (`Normalize` with a 100-row cap, `ToPagedResponseAsync` = COUNT + Skip/Take, `EscapeLike`/`TryBuildSearchPattern` escaping `\ % _` + length-capped) shared by every paginated endpoint; **`useDebouncedValue`** hook so the search box fires one request per pause. **Rotations:** `GET /api/rotations` now takes `page`/`pageSize`/`q` and returns `PagedResponse`; server ILIKE search mirrors the old client search (rotation number with/without the **R** prefix, student name/email, preceptor, specialty) with a **deterministic order** (StartDate desc, RotationNumber desc) so paging can't overlap/skip. `RotationsPage` drives page + debounced search server-side with **`keepPreviousData`** (flicker-free), an **"Updating…"/aria-busy** affordance, and a **page-clamp** (steps back if the set shrinks past the current page, gated on fresh non-placeholder data). **Full review roster** (backend/security/perf/frontend/tests) + adversarial verification, all findings fixed: built the **number-search pattern from the raw term** (not by slicing the escaped pattern); replaced a **tautological** pagination test with a real **disjoint-pages** test (3 rotations on a fresh program → page1/page2 disjoint, union == full list in order); **narrowed** the "contains seeded" assertions (q=R1001 / by created number) against the shared growing test DB; added preceptor-full-name-across-space (forces the concat ILIKE server-side), R9999 negative, length-limit accept/reject, page=0/pageSize=0 normalization, and frontend page-level tests (Next→page 2, delete-last-row→clamp to page 1, status-change→reset, empty state) + `useDebouncedValue` units. Security/perf: **PASS** (ILIKE escaping correct — backslash is Postgres's default escape; caps enforced; authz unchanged; OFFSET paging + non-sargable search acceptable at current volume, keyset/trigram noted as scale levers). 179 frontend tests pass; solution builds clean. **Out of scope (bounded data):** specialties, document-types, per-student documents. **Next slices:** students, preceptors, programs, campaigns (each needs an unpaginated options path for the form dropdowns that currently consume those list endpoints). |
| 2026-06-25 13:00 | [#67](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/67) | DEV | **Campaign send resilience** (FIX-2 — the cutover-hardening follow-up flagged in P3-4a). Made admin email-campaign sends **recoverable** instead of fire-and-forget. New **`CampaignRecipient`** (`crm`) per-recipient delivery log (Pending/Sent/Failed + classified error + attempt time; unique `(CampaignId, Email)`), which `SendCampaignJob` now **materialises idempotently** and processes **only still-Pending** recipients — persisting each as it goes, so a crash mid-send **resumes without re-sending** anyone already terminal; tallies are recomputed from the log. New **`EmailCampaign.SendStartedAtUtc`** stamps the claim time. New recurring **`CampaignSweepJob`** (every 5 min) recovers campaigns stranded short of terminal: **re-dispatches a Queued** whose enqueue was lost, and **resets+re-dispatches a Sending** that crashed (guarded on the stale claim time). Ran the **full `Docs/Review_Process.md` roster** (backend/security/data-concurrency/perf/tests, parallel + independent) + an adversarial verification pass. Findings verified and fixed: **(High)** the sweep could reset a *slow-but-alive* Sending run → a second concurrent fan-out (double-send) — closed by a **per-run deadline** (`MaxSendDuration` 10 min, strictly < the sweep's 15-min reset window), so a slow run always **bails before** the sweep can reset it (never two fan-outs); **(High security)** stop persisting raw `ex.Message` to the `Error` column (a real provider's text can embed the recipient address) → store a **classified, PII-free** reason; **(Medium)** the sweep reset now **stamps `ModifiedAtUtc`** so a just-requeued campaign isn't re-matched by the Queued sweep in the same pass; **(Medium)** materialisation tolerates a concurrent unique-violation. **HTML body sanitization deferred** with reason (plain-text body, composed in a textarea, **never rendered as HTML** in the SPA; the HTML-vs-text decision belongs with the real provider at cutover). Migration `AddCampaignRecipients` (`has-pending-model-changes` clean). Tests (CI): materialisation, **resume-no-double-send**, deadline-bail (stepped clock), strict sweep boundary (at/just-past cutoff), null-claim guard, same-pass single-dispatch regression, the `MaxSendDuration<SendingStuckAfter` invariant, partial-failure→Sent+FailedCount, sender-throws→PII-free Error+fan-out-continues, AllPreceptors, Failed-path claim clear. Solution builds clean (0 warnings). Deferred-with-reason: per-recipient SaveChanges batching (kept for crash-exact resume; ship as-is at current scale) and email-at-rest encryption (project-wide owner decision). |
| 2026-06-25 11:15 | [#66](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/66) | DEV | **Specialty delete guard** (FIX-1 — the follow-up flagged during P3-3). A specialty could be **soft-deleted while still referenced** by a live row (the FK `Restrict` only governs hard deletes; a soft-delete is an UPDATE), leaving it filtered out from under its referrers so any required-`Specialty`-navigation projection reads a **NULL name** (the dashboard reports query had to add a defensive filter to compensate). `DELETE /api/specialties/{id}` now returns **409** when a live **program** (`RotationProgram.SpecialtyId`) **or** a live **preceptor** (`Preceptor.PrimarySpecialtyId`) references it — the two (and only two) FKs to `Specialty` — mirroring the program- and student-delete guards. Adversarial review caught that the first cut only checked programs (missing the preceptor FK); **extended to both + re-reviewed exhaustive**. Tests: deleting a specialty a live program uses → 409 (and it stays); a live preceptor uses → 409; the existing unreferenced-delete still 204s. Builds clean. (Retires the defensive NULL-specialty filter's underlying gap; the filter can stay as belt-and-suspenders.) |
| 2026-06-24 08:30 | [#65](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/65) | DEV | **Admin dashboard "Campaign" tab — frontend** (PHASE 3-4b — the UI on top of the 3-4a backend; **completes all four PHASE 3 dashboard tabs**). The previously-placeholder **Campaign** tab (tab 2) now renders **`DashboardCampaignPanel`**: a **compose form** (subject ≤200, audience dropdown — all students / booked / not-booked / all preceptors, message ≤10000) that **saves a draft**, plus a **list** of campaigns with a status badge (Draft/Queued/Sending/Sent/Failed), the **sent/failed tally**, and sent-at; **drafts get a Send button** that queues the campaign for the Worker's send job (which fans out via the fake sender until cutover). `api.ts`: `EmailAudience`/`CampaignStatus` types + `CampaignSummary`/`CampaignDetail` + `getCampaigns`/`createCampaign`/`sendCampaign` (the send is a POST-no-body, matching the refund pattern). With all five tabs now built, the **"coming soon" fallback is removed** and the DashboardPage tab test asserts the Campaign panel renders. Adversarial frontend review → **sound, no fixes** (tab wiring correct; `send.variables===id` is the right per-row pending signal; type mirror exact incl. PascalCase enum serialization; POST-no-body handled; a11y labels unique; XSS-safe; no locale-dependent assertions). Tests: `DashboardCampaignPanel` (list + draft-only Send, save-draft validation→create, send→refetch, load error) + updated DashboardPage tab test. 172 frontend tests pass, **87.1% stmts**; build clean. **PHASE 3 dashboard surface complete: Results + ToDo's + Campaign + Reports + Revenue all live.** |
| 2026-06-23 18:20 | [#64](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/64) | DEV | **Build hotfix — unblock the Worker container publish (NETSDK1152)**, which also **brings P3-4a's campaign backend live on DEV** (deploy #64 had **failed** at the image-build step, so the `AddEmailCampaigns` migration + campaign code never reached DEV). Root cause: P3-4a's **Worker→API project reference** (for `RotationsDbContext`) transitively dragged the API's `appsettings.json` + `appsettings.Development.json` into the **Worker's** publish output, colliding with the Worker's own at the same relative path — `NETSDK1152`, which fails the SDK `DefaultContainer` publish. CI build-validation (only `dotnet build` + tests) stayed green and missed it — the collision is **publish-only**. Fix: a publish target on the Worker removing **only the API-sourced** `appsettings*.json` from `ResolvedFileToPublish` (matched by `RotationsPlus.Api` in the source path), keeping the Worker's own config **and** the API assembly (needed for the DbContext). Verified locally: Release publish succeeds, published `appsettings.json` is the Worker's, `RotationsPlus.Api.dll` present; DEV/PROD runtime config comes from env/Key Vault regardless. No behaviour change beyond the build. (Lesson: the Worker→API exe-project reference is a known shortcut — extract a shared `RotationsPlus.Data` project to retire it.) |
| 2026-06-23 17:40 | [#63](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/63) | DEV | **Email campaign backend + Worker send-job** (PHASE 3-4a — the backend half of the final dashboard tab; the first **api→Worker background-job** path). Composing/sending email campaigns to an audience, with the send running as a Hangfire job in the Worker. **Abstractions (Common):** **`IEmailSender`** + **`FakeEmailSender`** (logs + reports success, no real send) — the pre-cutover sender, swapped for a real provider by registration at cutover (mirrors `FakePaymentGateway` + the stealth rule); **`ICampaignSendJob`** — the job contract the API enqueues and the Worker implements (in Common, so neither references the other's concrete type → **no cycle**). **API:** **`EmailCampaign`** entity (`crm` schema) + migration `AddEmailCampaigns` (`has-pending-model-changes` clean); **`/api/campaigns`** (AdminOnly) create-draft / list / get / **send** (Draft-only → flips to `Queued` and dispatches via a thin **`ICampaignDispatcher`** seam over the Hangfire client — skipped in the Testing host). **Worker:** references the API for `RotationsDbContext` (pragmatic until a shared Data project is extracted; the API never references the Worker); **`SendCampaignJob`** resolves the audience (all / with-booking / without-booking students, all preceptors), fans out via `IEmailSender`, tallies sent/failed, moves to terminal. Adversarial review (rigorous on EF translation — CI-only — and concurrency): **no Critical/High**; the audience sub-query (`bookedStudentIds.Contains`/negation) translates cleanly and is NULL-safe; **fixed one Medium** — the read-then-write claim let two concurrent/retried runs double-send, replaced with an **atomic `ExecuteUpdateAsync` claim** (`Queued→Sending`, 0 rows ⇒ no-op); re-review clean. Larger **cutover** items (HTML sanitization of subject/body, dispatch-after-commit stuck-Queued, per-recipient delivery tracking) flagged as a follow-up. Tests: API integration (create/list/get, send queues + dispatches, non-draft 409, validation, AdminOnly 403/401) + **`SendCampaignJob` against the real Testcontainers DbContext** (all-students fan-out + counts, with/without-booking segmentation, all-fail→Failed, non-queued no-op idempotency) + 2 authz-matrix rows. 72 API + 1 Worker unit tests pass; full solution builds. **Frontend Campaign tab is 3-4b (next).** |
| 2026-06-23 13:10 | [#62](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/62) | DEV | **Admin dashboard "Reports" tab** (PHASE 3-3 — third net-new feature; read-only operational analytics). Fills the previously-placeholder **Reports** tab via **`GET /api/dashboard/reports`** (AdminOnly): the **booking-conversion funnel** (`totalStudents`, `studentsWithBooking` = distinct students with ≥1 live rotation, `totalRotations`), a **six-month registration trend** (students + preceptors per business month), and the **busiest specialties** by rotation volume (top 8). All counts over live (non-deleted) rows. Extracted a shared **`BusinessCalendar`** helper (zone + month-boundary math) now that three dashboard endpoints bucket by business month. Frontend **`DashboardReportsPanel`** (tab 3): funnel figures, registration trend bars, busiest-specialty bars. Adversarial backend+frontend review: **no Critical/High** — the conversion-overflow hypothesis (>100% via a soft-deleted student) was proven **unreachable** (three independent guards keep the numerator a subset of live students); applied the one **Low** fix (filter NULL specialty groups so the contract's non-null `SpecialtyName` holds, since specialty delete is unguarded) and **flagged the underlying specialty-delete-guard gap as a follow-up task**. Tests: integration (totals + 6-month trend ordering; a student registered today lands in the current month; a booked rotation converts the student + bumps its specialty; AdminOnly 403/401) + authz-matrix row + frontend panel (funnel, trend, specialties, zero-students guard, load error). 72 API unit + 168 frontend tests pass; **86.9% stmts**; builds clean. **Remaining dashboard tab: Campaign (needs an email vendor — to be built behind an `IEmailSender` abstraction, real sender deferred to cutover).** |
| 2026-06-23 11:25 | [#61](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/61) | DEV | **Admin dashboard "Revenue" tab** (PHASE 3-2 — second net-new feature; a money path, so it got a dedicated money-correctness adversarial pass). Fills the previously-placeholder **Revenue** tab via **`GET /api/dashboard/revenue`** (AdminOnly). All figures are **platform revenue** (deposits captured); preceptor **honorarium is a payout, not revenue, and is excluded**. **`collected`** = Σ `Amount` where `Succeeded` — and because a refund **flips the payment's status to `Refunded` in place**, that Succeeded sum *already* nets refunds out, so **`refunded`** (Σ `Amount` where `Refunded`) is reported **separately and never subtracted twice** (the classic double-count trap, avoided). Also **`outstandingReceivable`** (Σ `OutstandingAmount` where Succeeded), **`collectedThisMonth`**, a **6-month trend** bucketed by business month (US/Pacific, `TimeProvider`-driven), and **by-program-type** (Σ Succeeded grouped by type, which **sums to `collected`**). Everything is scoped to **live rotations** (`!p.Rotation.IsDeleted`) so the breakdown sums to the headline and no payment on a soft-deleted booking leaks via a NULL nav. Frontend **`DashboardRevenuePanel`** (tab 4): headline figures (Intl currency-formatted), the per-type breakdown, and a six-month collected bar trend. **Dedicated money-correctness review verified the figures correct** (no double-count, by-type-sums-to-collected structurally guaranteed by required FKs + non-null enum, decimal precision preserved end-to-end, server-side SQL aggregation, year-rollover month math sound, this-month == trend's last bucket, outstanding drops on refund and can't go negative, honorarium never mixed in); frontend review clean — **no code changes required**. Tests: integration (captured deposit → collected/outstanding/by-type/this-month/trend; refund moves money collected→refunded without double-counting; by-type-sums-to-collected invariant; AdminOnly 403/401) + authz-matrix row + frontend panel (figures, breakdown, trend, load error). 72 API unit + 164 frontend tests pass; **86.8% stmts** coverage; builds clean. **Remaining dashboard tabs: Reports + Campaign (Campaign needs an email vendor — to be built behind an abstraction, real sender deferred to cutover).** |
| 2026-06-23 10:05 | [#60](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/60) | DEV | **Admin dashboard "ToDo's" tab** (PHASE 3-1 — the first net-new, beyond-parity feature). The dashboard's previously-placeholder **ToDo's** tab now surfaces the admin's actionable work queues via a new **`GET /api/dashboard/todos`** (AdminOnly): **documents to review** (RotationDocuments in `Submitted`, oldest submission first), **awaiting payment** (rotations still `Pending`/deposit unpaid, soonest start first), and **preceptor approvals** (preceptors `Pending`, longest-waiting first). Each bucket returns the full outstanding **count** (the tab badge) + a capped **Take(10)** deterministically-ordered preview. Frontend **`DashboardTodosPanel`** renders the three queues with count badges, an **all-clear empty state**, and a **"+N more"** link into the owning admin screen (`/admin/students`, `/admin/rotations`, `/admin/preceptors`) when the count exceeds the preview. Adversarial backend+frontend review: frontend clean; backend caught **one real Medium — fixed**: a rotation's soft-delete doesn't cascade to its documents, so a `Submitted` doc on a soft-deleted rotation would surface as a **dangling NULL-nav row** (NULL into the non-nullable `RotationNumber` → 500; count/preview mismatch) — fixed by constraining the shared docs query to **live rotations** (`!d.Rotation.IsDeleted`) + a regression test (the doc drops from both count and items once its rotation is deleted). Re-review confirmed sound. Tests: integration (each queue surfaces its row with a matching count; soft-deleted-rotation exclusion; AdminOnly 403/401) + authz-matrix row + frontend panel (queues+counts, "+N more" href, empty state, load error). 72 API unit + 160 frontend tests pass; **86.8% stmts** coverage; solution builds clean. **First slice of the Reports/ToDo/Campaign/Revenue dashboard surface; the other tabs remain "coming soon".** |
| 2026-06-22 22:35 | [#59](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/59) | DEV | **Admin per-student document review modal** (PHASE 2g-3c; the second admin document form — completes the 2g-3 admin UI). Rewrite of the legacy Student-Profile **"Documents" tab**: a new **"Documents"** row action on the Students admin opens a **`StudentDocumentsModal`** listing **every document across the student's rotations** (`GET /api/students/{id}/documents`) with — a **status dropdown** (the review action: non-rejected statuses apply immediately; selecting **Rejected** reveals a **required rejection-reason** input + Save, and an already-rejected doc keeps the reason editable), the **uploaded-file link** (short-lived read SAS), **upload/replace + remove on behalf** of the student (multipart → the 2g-3a endpoints), and a **filter by rotation number** (shown when the student has >1 rotation). Adds a staff-token **`requestUpload`** multipart wrapper + the admin document API functions to `api.ts` (mirroring the 2g-3a contracts; `requestUpload` uses the **workforce** token, endpoints are AdminOnly). Adversarial frontend review caught **one real HIGH — fixed**: a student can require the **same document type in two rotations**, so the type name alone wasn't a unique accessible name — the four derived `aria-label`s collided (a11y defect + `getByLabelText` throw on realistic data); fixed by **qualifying every label with the rotation number** ("… (R1001)") + a regression test; also applied the review's LOW (make the rejection reason **re-editable** for an already-rejected doc, since re-picking the same `<select>` value doesn't fire `onChange`). Re-review → ship-ready (one optional concurrent-edit reason re-sync noted as non-blocking LOW follow-up). Tests: `StudentDocumentsModal` (list+rotation context, approve-on-select, reject-needs-reason, **same-type-two-rotations label disambiguation**, upload-on-behalf, rotation filter, load-error). Frontend build clean, **87.0% stmts** coverage (gate 70%); 156 frontend tests pass. **PHASE 2g admin UI complete** (2g-3b config + 2g-3c review); the document subsystem MVP is now end-to-end (config → materialize → student upload/track → admin review). |
| 2026-06-22 19:30 | [#58](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/58) | DEV | **Program required-documents config UI — admin** (PHASE 2g-3b; the first admin document form, on top of the 2g-3a backend). Rewrites the legacy "Required Documents" admin form, placed on the **Program** (owner decision — matches the per-program data model; due-days lives on the program). Reachable from a new **"Required documents"** footer button in the edit **Program** modal, it opens a **`ProgramDocumentsModal`**: the **document-type catalog grouped by the 8 categories** as a checkbox checklist, an **add-custom-type** control (name + category → catalog, auto-selected), the per-program **documents-due-days** input (0–365), and **Save** through the full-replace `PUT /api/programs/{id}/required-documents`. Adversarial frontend review caught **two real bugs — both fixed**: (HIGH) adding a custom type called `config.refetch()`, which re-fired the state-seeding effect and **clobbered the admin's in-progress checkbox/due-days edits** (and dropped the just-added auto-selection) — fixed by seeding local state **exactly once** (`useRef` guard) and **appending the new type into the cached catalog via `setQueryData`** instead of refetching; (MEDIUM) clearing the due-days input sent **`NaN` → `null`** to the backend — fixed by coercing empty to 0 and disabling Save out of range. Re-review clean, no further changes. Tests: `ProgramDocumentsModal` (catalog render + configured selection, toggle→save with due-days, **add-custom-type preserves in-progress edits with no refetch** (regression), load-error). Frontend build clean, **87.7% stmts** coverage (gate 70%); 149 frontend tests pass. **Admin document UI next: 2g-3c** (Student-row review modal — the per-rotation Status dropdown + upload/delete-on-behalf). |
| 2026-06-22 16:17 | [#57](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/57) | DEV | **Admin document review + required-docs config — backend** (PHASE 2g-3a; the admin side, after the owner sent the legacy admin forms — Student-Profile Documents tab, Program required-docs, Preceptor due-days). Decisions from the forms: required-docs config lives on the **Program** (matches the per-program data model; due-days moved to the program), formats expand to the **full legacy set**, and the per-student review is a **modal** (the new admin is list+modal). Adds: **configurable `RotationProgram.DocumentDueDays`** (default 14, replaces the hardcoded constant; materializer now reads it; migration `AddProgramDocumentDueDays`, `has-pending-model-changes` clean); a **shared magic-byte `DocumentContentTypeDetector`** accepting **PDF/DOC/DOCX/PNG/BMP/JPG**, used by **both** student + admin uploads (client content-type still never trusted; DOCX==ZIP limitation documented, download-only); **admin review endpoints** (AdminOnly) — `GET /api/students/{id}/documents` (with rotation context + read URLs), `PUT /api/documents/{id}/status` (review dropdown + rejection reason, stamps reviewer, **Enum.IsDefined-guarded**), `POST .../file` (upload on behalf), `DELETE .../file` (clear → UploadNeeded); **required-docs config endpoints** (AdminOnly) — `GET/PUT /api/programs/{id}/required-documents` (full-replace reconciliation over the partial-unique index, due-days), `GET/POST /api/document-types` (catalog + add custom, case-insensitive dup → 409). Adversarial review verified the **soft-delete reconciliation** (re-add across the partial unique index) and **EF translation** — clean; fixed an out-of-range-status guard + added the re-add round-trip test (the one CI-only path). Tests: `AdminDocumentEndpointTests` + `ProgramDocumentConfigEndpointTests` + **8 authz-matrix rows**; 72 API unit tests; solution builds clean. **Admin UIs next: 2g-3b** (Program form required-docs config) **+ 2g-3c** (Student-row review modal). |
| 2026-06-22 12:29 | [#56](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/56) | DEV | **Student document upload UI** (PHASE 2g-2b; the portal half — completes the student document loop end-to-end). The My-rotations **"Documents" column** now reflects the real **`DocumentsState`** — **"Documents Missing"** (red link) / **"All Documents Uploaded"** / **"—"** — replacing the placeholder, and opens a per-rotation **`DocumentsModal`** checklist: each required document with a **status badge** (Upload needed / Submitted / Approved / Rejected / Expired), due date, the **uploaded-file link** (short-lived SAS), the **rejection reason** when rejected, and an **Upload/Replace** control (PDF/JPEG/PNG → the 2g-2a multipart endpoint). **Approved docs are locked** (no re-upload control, matching the server 409). On a successful upload it refetches the checklist and **invalidates the rotations query** so the column flips live. Adds an **`apiUpload`** multipart helper (no `Content-Type` so the browser sets the boundary) + the customer document API/types. Adversarial frontend review → ship-ready (contracts mirror the C# exactly; FormData handling, query-key wiring, and approved-lock all correct; XSS/reverse-tabnabbing safe — `target=_blank rel=noopener`, React-escaped text); one polish applied (clear the success banner when a new upload starts). Tests: `DocumentsModal` (checklist render, approved-lock, upload→refetch, error) + `MyRotationsPage` (the three column states + opens the checklist). Frontend build clean, **87.8% stmts** coverage (gate 70%). **Document subsystem MVP is now student-complete** (data model + materialization + upload/serve + tracker UI); the remaining piece is the **admin** config + review UI (**2g-3**, awaiting the owner's exact admin forms). |
| 2026-06-22 10:51 | [#55](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/55) | DEV | **Document upload + serve — backend** (PHASE 2g-2a; the storage half of the student upload, frontend portal UI is 2g-2b). Adds a private **`documents`** blob container on the same storage account as program images (Bicep; idempotent container add, **reuses the existing `blob-connection` Key Vault secret** — no new secret), an **`IDocumentFileStore`** (Azure account-key service-SAS + in-memory fallback, registered by config presence) mirroring the PHASE 2b image store, and the upload endpoint **`POST /api/customer/rotations/{rid}/documents/{docId}/file`**: **CustomerOnly + oid ownership** (the document's student must carry the caller's oid, and the doc must belong to the route's rotation — no IDOR), multipart, **magic-byte-validated** PDF/JPEG/PNG (client `Content-Type` never trusted; the sniffed type is what's stored *and* served), **10 MB** cap (early body-size limit → 413), save-then-delete on replace. Submitting flips **`UploadNeeded→Submitted`**, stamps `SubmittedAtUtc`, and clears any prior rejection; the customer checklist now returns a **short-lived read URL** (`FileUrl`) per uploaded file. Adversarial security review (auth/IDOR, magic-byte, SAS blob-scope, save/delete ordering, Bicep idempotency) → ship-ready; it surfaced one real item — re-uploading over an **Approved** doc silently re-opened it and wiped the reviewer audit — **fixed** by locking an approved doc against student replacement (409; Rejected/Expired/etc. stay re-uploadable). Tests: `DocumentUploadEndpointTests` (submit→read-url, re-upload replace, magic-byte reject, cross-student 404, approved-lock 409, authz) + authz-matrix row; 72 API unit tests; solution + Bicep build clean. **No new rate limit on upload** (10 MB cap + re-upload-replaces-blob bound per-doc storage; matches the image-upload precedent — generalize per-customer limit when SEC-2 is extended). |
| 2026-06-22 08:50 | [#54](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/54) | DEV | **Document subsystem — backend foundation** (PHASE 2g-1; first slice of the owner-approved MVP document feature). Stands up the rewrite of the legacy document mess (5+ overlapping tables, OCR-via-SFTP, HelloSign e-sign, PAL letters) as a clean core loop in a new **`documents`** schema: **`document_types`** (seeded ~14-type catalog, replaces 50+ free-text strings), **`program_required_documents`** (per-program required-docs config, replaces the legacy `program.require_documents` JSON; seeded on the Internal-Medicine program), and **`rotation_documents`** (per-rotation instance with an explicit `DocumentStatus` state machine — `UploadNeeded→Submitted→Approved|Rejected`/`Expired` — collapsing the legacy status soup). On **both** booking paths (admin create + customer self-book) the program's required docs are **materialized** (one `UploadNeeded` row per type, due 14 days before start). The student-facing **"Documents" tracker column** is now a computed **`DocumentsState`** (`NotRequired`/`Missing`/`Complete`) on `CustomerRotationResponse` (an EXISTS projection), and a customer read endpoint **`GET /api/customer/rotations/{id}/documents`** (CustomerOnly + oid ownership) returns the per-rotation checklist. Migration `AddDocuments`, `has-pending-model-changes` clean. **Deferred to later slices:** student upload + Blob file-serve (**2g-2**, reuses the 2b `program-images` account-key SAS pattern), and the admin **required-docs config + review UI** (**2g-3**, *blocked on owner-provided exact admin forms*). Also deferred as separate phases: OCR validation, HelloSign e-sign (removed vendor), PAL visa letters (paid add-on), deadline-reminder emails. Tests: `DocumentEndpointTests` (materialization count, Missing state, ownership isolation, self-booking, authz) + authz-matrix row; 72 API unit tests; solution builds clean. Adversarial review gave a **definitive yes on the EF→SQL translation** of the nested-ternary correlated-EXISTS `DocumentsState` projection and verified migration/seed FK integrity, materialization timing (client-gen v7 GUID, same transaction), and no regression to the existing booking/payment suites — no code changes. |
| 2026-06-21 16:47 | [#53](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/53) | DEV | **Performance — rotation indexes, dashboard query batching, no-tracking detail** (PHASE 2f). Driven by an adversarial perf audit (every list/detail/dashboard query vs the index set; budget API p95 < 300ms). **PERF-2a:** replaced the `rotations` Status-only index with a composite **`(Status, StartDate)`** partial index — serves the admin list's **status filter + start-date sort** *and* the dashboard's today-cycle counts — plus a **`StartDate`** partial index for upcoming-starts / date-cycle; both **partial on `NOT IsDeleted`** to match the global soft-delete filter (smaller, planner-aligned; the Postgres partial-predicate matcher proves `"IsDeleted" = false` ≡ `NOT "IsDeleted"`). Migration `AddRotationDateIndexes`, `has-pending-model-changes` clean. **PERF-3:** collapsed the dashboard's **four** today's-cycle `COUNT` round-trips into **one** `COUNT(*) FILTER (WHERE …)` grouped aggregate (13 → 10 queries on the admin hub); kept on a single `DbContext` (not parallelized — DbContext isn't thread-safe). **PERF-4:** `AsNoTracking()` on the read-only `GET /api/rotations/{id}`. **PERF-1 (list pagination)** is **split into its own slice** — it's a breaking response-envelope change touching ~6 SPA pages (the frontend currently paginates client-side over the full list), so it gets a dedicated coordinated backend+frontend PR rather than bundling here. Tests: `RotationIndexTests` asserts the new indexes exist (and the old one is gone) against the Testcontainers Postgres; the existing dashboard cycle test exercises the batched `FILTER` aggregate. 72 API unit tests; solution builds clean. Adversarial review gave a **definitive yes on the EF→SQL translation** of the filtered-aggregate (same predicates already ship on develop; `GroupBy(_=>1)` is the supported aggregate shape; `HasConversion<string>` doesn't impair the `ANY` translation) — no code changes. |
| 2026-06-21 14:30 | [#52](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/52) | DEV | **Security hardening — scheme-pinned policies, rate limiting, ProblemDetails** (PHASE 2e). Driven by an adversarial security audit of the whole API (every endpoint × policy enumerated; IDOR/mass-assignment/CORS/webhook-signature/soft-delete/money-overflow all verified safe). Three fixes: **SEC-1** — each authorization policy is now **pinned to its authenticating JWT-bearer scheme** (StaffOnly/AdminOnly→workforce, CustomerOnly→customer, MarketplaceViewer→both), so a token validated by the *wrong* Entra directory can't satisfy a policy even if a role name were ever shared (defense-in-depth atop the disjoint-role invariant); the integration harness forwards the real scheme names to `TestAuthHandler` (mutates `SchemeMap[…].HandlerType`) so pinned policies still authenticate in tests. **SEC-2** — **rate limiting** (`AddRateLimiter`): a **per-IP** fixed window on the anonymous `/api/webhooks/stripe` (the one unauthenticated write surface, previously floodable into HMAC+DB work) and a **per-user (oid)** window on the money paths (open-deposit + self-booking); config-bound (`RateLimiting:*`) with generous defaults (240/120 per 60s) so the suite never false-trips; rejections return **429 + Retry-After**. **SEC-3** — a uniform **RFC 7807 ProblemDetails** contract (`AddProblemDetails` + `UseExceptionHandler`) replaces ad-hoc/empty 500s; 429s carry a ProblemDetails body too. **Deferred (documented):** `AllowedHosts` tightening (DEV still on Azure default hostnames until custom DNS at offboarding) and real Stripe `Stripe-Signature` parse/replay-tolerance (gateway-swap cutover checklist). Tests: `SecurityHardeningEndpointTests` (webhook 429 burst + ProblemDetails/Retry-After contract) + the full **authz-matrix** suite re-validates every endpoint×role under the pinned policies. 72 API unit tests; solution builds warnings-as-errors clean. Adversarial audit + an independent verification re-review (which empirically ran the rate-limiter config to confirm 429/Retry-After/`problem+json`) — no blocking findings. |
| 2026-06-21 13:40 | [#51](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/51) | DEV | **Dashboard per-type programs + Today's LiveScore metrics** (PHASE 2d). Expands `GET /api/dashboard` (AdminOnly) to back the admin **LiveScore** cards end-to-end, removing the `0`/`—` placeholders left by the #41 frontend clone. Adds **`ProgramsByType`** — the full per-delivery-type catalog breakdown (the SPA's `programFamilyCount` folds research/sub variants into the **InPerson/Consultation/TeleRotation** rows it shows; **Dental folds out**, so the circle can exceed the three badges by the Dental count, documented at the render site) — and a **`Today`** block: **new programs (by type), new students, new preceptors**, and the **rotation cycle** (`Starting`/`InProgress`/`Completing`/`Cancelled`). "Today" is the **business day in US/Pacific** (matching legacy), driven by `TimeProvider`; the cycle buckets are **mutually disjoint** (a same-day rotation counts only as *Starting*; *Completing* excludes it; *InProgress* is strictly mid-flight) so they sum cleanly to the circle. Per-type/new-by-type totals are computed from the **same grouped query** (sum-of-buckets), so the breakdown can't drift from the headline. **Issues Reported = 0** until an issues subsystem exists. No model/migration change (`has-pending-model-changes` clean). Tests: integration (per-type sum invariant + `Today` consistency, a program created today appears in new-by-type + all-time totals, a rotation starting today increments the cycle) + frontend (per-type breakdown family-summing, Today's movement, cycle circle). 90 frontend dashboard assertions pass, 89% stmts coverage; 72 API unit tests; adversarial **backend + frontend** review → **fixed a DST-transition-day off-by-one-hour** in the start-of-day window (resolve the offset for the date's midnight, not the current instant), re-review clean. |
| 2026-06-21 12:55 | [#50](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/50) | DEV | **Rotation Number — sequential, on My-rotations + admin** (PHASE 2c). Replaces the "Rotation Number" placeholder on the customer **My-rotations** tracker and the admin **Rotations** list with a real value, mirroring the shipped **ProgramNumber** pattern: a server-assigned sequential **identity** column (`Rotation.RotationNumber`) shown as **"R{number}"** (the rewrite analog of the legacy `'R'+id`, not the raw GUID). Migration `AddRotationNumber` seeds the existing rotation to **1001** and **restarts the sequence at 2000** (literal quoted-identifier `ALTER COLUMN … RESTART WITH`, per the PHASE 2a lesson) so new rotations never collide; `has-pending-model-changes` clean. `RotationNumber` added to the admin summary/detail + **customer** rotation DTOs and all four projection sites (admin list+detail, customer list + booking response). Frontend: `R{number}` on both screens, and the admin **search box now actually matches the number** (both "R1001" and "1001"), honouring its "Search for Number/Preceptor/Student" placeholder. Tests: backend (seeded=R1001 on list+detail; created/booked > 1001) + frontend (renders on both screens; admin search-by-number filters). 138 frontend + 72 API unit tests; adversarial review (migration + projection-order focused) clean. **The "Documents" column stays a placeholder** — it's a computed status over a documents subsystem that doesn't exist yet (its own phase, per Plan_Architecture §304), not a field-fix. |
| 2026-06-21 09:50 | [#49](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/49) | DEV | **Program hospital images — portal wiring** (PHASE 2b-ii, frontend; the UI half, on top of 2b-i's storage backend). Wires the catalog **`imageUrl`** (the short-lived read SAS from #48) into the cloned **Browse** card and **Program-detail** hero, matching the legacy `hospital_image` presentation — **`object-fit: cover`** filling the image box, the gray **`#f5f3f3` placeholder** showing through when there's no image. A shared **`ProgramImage`** component renders the photo and **falls back to the placeholder** when `imageUrl` is null **or** the image fails to load/expires. **No fake placeholder image** (owner decision) — real hospital images arrive with the **production data migration** and then render with zero further work. `api.ts` `Program`/`ProgramDetail` gain optional `imageUrl`; the admin **Programs** list image column is unchanged (still its own placeholder for now). Adversarial review **caught + fixed a real bug**: an imperative `onError → display:none` persisted on the reused `<img>` node across `src` swaps, so — because the API mints a **fresh SAS string every fetch** — a refetch's new valid URL stayed hidden; replaced with a **state-driven `failed` flag reset on URL change** (also DRYs both usages, avoids re-downloading on refetch). Tests: `ProgramImage` (renders / no-url / hides-on-error / **revives-on-new-url**) + Browse (image vs placeholder) + detail (hero src). 137 frontend tests, 90.8% lines; build clean. **PHASE 2b complete** (storage backend + portal UI). |
| 2026-06-21 04:55 | [#48](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/48) | DEV | **Program hospital images — Blob storage + serve path** (PHASE 2b-i, backend; the storage half of the catalog-image work). Adds **`RotationProgram.ImageBlobName`** (migration `AddProgramImageBlobName`) and an **`IProgramImageStore`** abstraction with an **Azure Blob** impl (uploads + mints short-lived **read service SAS** URLs from the account key) and a functional **in-memory** stand-in used when no storage connection is configured (local dev / integration tests) — registered by config presence, mirroring `IPaymentGateway`. **Infra (Bicep):** a new `storage.bicep` module provisions a **private** StorageV2 account (no anonymous blob access, TLS1.2, HTTPS-only) with a `program-images` container; `main.bicep` seeds the **account-key connection string into Key Vault** (`blob-connection`) and injects it to the API as `Storage__Images__ConnectionString`. Account-key SAS (not managed-identity user-delegation) is deliberate — the deploy pipeline principal is only **Contributor** and can't create the role assignment user-delegation needs; the key never leaves Key Vault/the container. **API:** admin-only **`POST`/`DELETE /api/programs/{id}/image`** (multipart; **magic-byte-validated** JPEG/PNG/WebP, ≤5 MB, early body-size cap → 413). Catalog **list + detail** now project **`ImageUrl`** (a read SAS, **null when no image**). **No fake images** (owner decision): `imageUrl` stays null until the **production data migration** brings real hospital images, at which point they render with zero further work. **Tests:** integration (upload→list/detail reflects it, delete clears it, mislabelled-non-image→400, content-type/size/missing/unknown-program guards, non-admin→403) + 2 authz-matrix rows; unit (Azure read-SAS construction + connection-string parsing, exercised offline). Adversarial **security + correctness** review → **magic-byte sniffing** (don't trust the client Content-Type) + early **request-body cap** added; storage-name length headroom fixed; re-review clean. **Frontend wiring** (Browse card + detail hero, placeholder fallback) follows in **PHASE 2b-ii**. |
| 2026-06-20 17:15 | [#47](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/47) | DEV | **PHASE 2 kickoff — program catalog backend fields** (PHASE 2a; first backend-fix for the cloned UIs). Adds **`ProgramNumber`** (Postgres `GENERATED BY DEFAULT AS IDENTITY`, server-assigned; the migration advances the sequence past seeds+back-fill via `setval(GREATEST(2000, max+1))` so a new number can never collide), **`City`/`State`**, and **`Tags`** (`text[]`) to `marketplace.programs`. Projects them through the catalog **list + detail** DTOs; admin **create/update** accept location + tags with **trim / case-insensitive de-dupe / size bounds** (≤20 tags, ≤60 chars each). **"Instant Approval" is derived from the existing `IsOpen`** at projection time (never stored as a tag, so flag and tag can't disagree). Migration `AddProgramCatalogFields` + seed sample values. **Frontend**: a `programCode` helper (legacy **IP/CS/TL** prefix + number) wires the real **program code**, **city/state location**, **seats**, **Instant-Approval**, and **tag chips** into the portal **Browse** + **Program Detail** cards and the admin **Programs** list — removing those placeholders. **No hospital image yet** (Blob = PHASE 2b; real images + real catalog values arrive with the **production data migration**). Integration tests added (catalog fields on list/detail, tag normalization, too-many-tags→400); the migration is exercised by CI's Testcontainers Postgres. Adversarial review (migration-focused, since migrations are CI-only locally) → made the sequence-restart dynamic. 132 frontend tests, coverage 91% lines; backend builds. **Cutover note:** the DataMigrator must carry each legacy `program_id` into `ProgramNumber` (explicit insert — the column is `BY DEFAULT`, not `ALWAYS`) and then bump the sequence past the max, so the rewrite's user-facing codes match the codes customers/preceptors already know. |
| 2026-06-20 13:10 | [#46](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/46) | DEV | **Portal sign-in — on-brand landing** (PHASE 1; final portal screen — **completes the customer-portal clone**). Restyled the unauthenticated portal landing into an **on-brand welcome hero** (logo, PT Serif headline "Find your clinical rotation", brand gradient backdrop, brand CTA) instead of the plain card. Customer credentials are entered on the **Entra External ID hosted page** (the combined `rplus-susi` sign-up-or-sign-in flow), so this screen is a **branded entry point, not an in-app credential form** — there is no legacy login modal to clone (the legacy email/password modals don't apply to the CIAM redirect model). Redirect handler switched to async/await try-catch; added `CustomerSignIn` tests (render + click-triggers-redirect). Adversarial review **caught + fixed a staff-signin regression** (the restyle had removed shared `.signin .card` rules that `pages/SignIn.tsx` still relied on — restored, scoped). 132 frontend tests (2 new), coverage 91% lines; build clean. **PHASE 1 (clone all existing frontends) is now complete** — admin shell + Programs/Rotations/Students/Preceptors/Specialties/Dashboard/Overview, and the customer portal Browse/Program-Detail/My-Rotations/Sign-in. Next: **PHASE 2 — fix the backends behind these screens** (placeholder fields + security/perf audit). |
| 2026-06-20 10:55 | [#45](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/45) | DEV | **Legacy UI clone — customer portal My rotations** (PHASE 1; portal screen 3). Restyled the portal My-rotations page to clone the live student **"Rotations Tracker"** (legacy `StudentDashboard.js`) — a `.div-table` of **labelled `responsive-row` cards** (Program ID · Specialty · Type · Preceptor · Rotation Number · Start Date · End Date · Weeks · Documents · Status) with **"Show more (N)"** progressive reveal (+5), replacing the earlier generic stat-card grid. The **working Pay-deposit money path** (Pay deposit on a Pending rotation → PaymentModal → deposit → Approved) is unchanged — **frontend-layout-only, no backend changes**. Status keeps the **NotStarted→"Approved"** student-facing label. Program ID / Rotation Number / Documents render as **placeholders** (the customer rotations API doesn't expose them yet). Adversarial review (correctness + legacy-fidelity) clean; one cosmetic tidy (action cell only rendered when there's an action). 130 frontend tests (preceptor assertion updated to the new column layout), coverage 91% lines; build clean. |
| 2026-06-20 09:25 | [#44](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/44) | DEV | **Legacy UI clone — customer portal Program Detail** (PHASE 1; portal screen 2). Restyled the portal program-detail page to clone the live **"Program Details"** screen from the legacy `RotationDetail.js` (Figma `317:9785` is the **deposit** variant; Figma API is rate-limited ~3.6 days so this was built from the legacy live-code + the existing Figma tokens, owner-approved method). Layout: a **hero band**, a **detail card overlapping the hero**, a header with **tag pills** (program code · type · seats) + **title** + **total price** + the **"For N weeks minimum"** line + the **"10% Approval Deposit Required"** line, a **two-column body** (placeholder hospital image + **Preceptor** block + the **booking panel** on the left; blue **tag chips** + the full **labelled details grid** (Specialty · Affiliated Hospitals · Sub-specialty · Inpatient · LOR Letterhead · Max Students · Designation · Min weeks · Privileges · Required Documents · Research · Topic) + **Description** + **Reviews** on the right), and a **Call/E-mail contact footer**. The **working booking mechanism is unchanged** (start date + weeks + live server quote + Book → My rotations) — **frontend-layout-only, no backend changes**. **ConsultationSub** priced **hourly** (rate as-is, no "/ week", no seats pill). Header **price = whole-minimum-stay total** (per-week × min weeks), matching the live site. Fields the catalog API doesn't expose yet (program code, hospital image, city/state, seats, affiliated hospitals, sub-specialty, letterhead, privileges, documents, research/topic, program tags, reviews, preceptor-unlock) render as faithful **placeholders**. Two-angle adversarial review (correctness clean; legacy-fidelity → header-total + min-weeks-line fixed). 130 frontend tests (1 new), coverage 91% lines / 79% branch; build clean. **Notes/deferred:** (a) the program-type **label vocabulary** ("In person" vs live "InPerson"; "Consultation (sub)" vs collapsed "Consultation") lives in the shared `programTypes.ts` used by admin screens — a cross-cutting fix for its own pass; (b) the **no-deposit/open** program variant is a separate Figma frame (`1549:9674`) and gating the deposit line on `isOpen` needs the customer DTO to expose it (PHASE 2). |
| 2026-06-20 07:55 | [#42](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/42) | DEV | **Legacy UI clone — customer portal Browse** (PHASE 1; first of the portal screens, a distinct public/student-facing visual target). Rebuilt the portal Browse page from the **Figma frame** (`317:9278`) + the legacy `SearchResults.js`/`RotationItem.js`: a **hero band** ("Search Results" + the **frosted search-pill row** — text · specialty · type · max-$/wk + a pink SEARCH), a **"Sort by:"** control, and a **3-up grid of rotation cards** cloned from the live card — hospital image + favorite heart, two pink **tag pills** (program code · type), PT-Serif **title**, blue-pin **location** row, **total price** + star **rating**, the **minimum-weeks** line, and a **specialty** pill — with **client-side price sort** and **"Show more"** (one click reveals all, live parity). Cards use an **accessible stretched-link** pattern (whole card clickable + keyboard-navigable, heading preserved). **ConsultationSub** is priced **hourly** (per-week amount, "Hourly" duration), matching the legacy. **Frontend-layout-only**: fields the catalog API doesn't expose yet (program code, hospital image, city/state, rating, favorites) render as faithful **placeholders** (—, (0), gray stars); specialty/type/price/preceptor/min-weeks are real. Two-angle adversarial review (correctness + legacy-fidelity) → 7 findings fixed, clean re-review. 129 frontend tests (2 new), coverage 91% lines / 79% branch; build clean. **Note:** the program-type label vocabulary ("In person" vs the live "InPerson") lives in the shared `programTypes.ts` used by already-shipped admin screens — a cross-cutting fidelity fix to be done in its own pass, not here. |
| 2026-06-20 06:15 | [#41](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/41) | DEV | **Admin Dashboard — proper clone (fix)** — the earlier "dashboard" pass (#40) only dropped the page-head and left the old stat-cards, which looked nothing like the live dashboard. Rebuilt from the **Figma frame** (`317:18081`) + the legacy `dashboard/admin/index.js`: an **Upcoming-Starts month calendar** (Mon-first grid, prev/next nav, pink event-dots on days with a start, today highlighted), the **Student/Specialty/Starts/Status** table, a **Results/ToDo's/Campaign/Reports/Revenue tab bar** (non-Results → "coming soon"), a **Today's LiveScore** card, and a **LiveScore totals** card — using the Figma blue `#5AA6FF` circle/pill/badge widgets + PT Serif numbers. Real data wired where the API provides it (Total Programs/Students/Preceptors/Specialties/Rotations, and the rotations breakdown In-progress/Active/Completed from the by-status counts, Waiting-for-Approval from Pending); the **daily "Today's" metrics + per-program-type breakdowns are placeholders (0 / —)** pending backend metrics (frontend-only pass). New reusable circle/pill/badge + calendar CSS. 128 frontend tests (Dashboard test rewritten for the new structure), coverage ~90%; build clean. |
| 2026-06-19 21:22 | [#40](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/40) | DEV | **Legacy UI clone — Dashboard + Overview** (PHASE 1, screens 6–7; completes the **admin** side of the frontend clone). Both already inherit the cloned shell (sidebar/header/footer) + the global Figma tokens (serif headings, PT Sans, brand), so the rework was dropping each page's redundant `page-head` (the screen title now lives in the cloned header bar) and letting the stat-cards/identity panels sit in the cloned aesthetic. No behaviour change; the Dashboard's totals/by-status/upcoming-starts and the Overview identity card are unchanged and still covered by their tests (zero test changes). 127 frontend tests, coverage 90% stmts / 80% branch; build clean. **Remaining PHASE 1:** the customer **portal** (Browse · Program detail · My rotations · sign-in) — a distinct visual target (the public student-facing site), tackled next. |
| 2026-06-19 21:05 | [#39](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/39) | DEV | **Legacy UI clone — Students · Preceptors · Specialties** (PHASE 1, screens 3–5; batched as one PR since they're the same near-identical list-screen transform). Each admin directory page restyled into the live app's look using the cloned shell + the Programs/Rotations row-card pattern: a **toolbar** (Add + status filter where applicable + a "Search for …" box), **labeled row-cards** in the cloned `program-table` style, and **client-side pagination**. As with Rotations, the restyle **preserved every DOM anchor** (table/rows, Edit/Delete actions, status filters, badges), so all existing component tests pass **unchanged**. 127 frontend tests, coverage 90% stmts / 80% branch; build clean. Nav placement for these three (not top-level items in the live admin nav) remains the open IA question — still reachable by URL; to be resolved as the legacy Data/member-management areas are cloned. |
| 2026-06-19 19:40 | [#38](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/38) | DEV | **Legacy UI clone — Rotations** (PHASE 1, screen 2 of the frontend rollout; owner confirmed Programs as the reference). Frontend-only restyle of the admin Rotations page into the live app's look, reusing the cloned shell + the Programs row/search/pagination patterns: a **toolbar** (Add rotation · Status filter · "Search for Number/Preceptor/Student"), **labeled row-cards** (Rotation Number · Student · Preceptor · Specialty · Type · Dates · Weeks · Status badge) in the cloned `program-table` style, and **client-side pagination**. The restyle **preserved every DOM anchor** (table/rows, the Edit/Refund/Delete actions, the Status filter, the status badge), so all existing behaviour — incl. refund-gating (Cancelled/Completed only), the status-machine edit dropdown, delete/refund confirms — is unchanged and still covered by the existing 21 rotation tests with **zero test changes**. Rotation Number is a placeholder ("—") pending the backend slice. 127 frontend tests, coverage 90% stmts / 80% branch; build clean. |
| 2026-06-19 13:25 | [#37](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/37) | DEV | **Legacy UI clone — admin shell + Programs (reference screen)** (owner decision 2026-06-19: reproduce the live app UI to ~99%; clone-everywhere is now the standing direction, Programs is the reference for sign-off before rollout). **Frontend-only** this pass (no backend changes): the admin shell is rebuilt to mirror the live app — a rounded **icon sidebar** (Dashboard · Analytics · Permissions · Rotations · Honorarium · Contacts · Programs · Leads · Customer Service · Admin · Data; wired items navigate, not-yet-built ones render inert), a header with the page title + user dropdown (sign-out), and an approximated marketing footer (legacy icons/logo copied into the app). The **Programs page** is cloned: program-type **tabs** (InPerson/InPersonResearch/Consultation/TeleRotation/TeleResearch), a **filter + "Search for Programs"** toolbar, **image rows** (hospital thumbnail, program-name, labelled Program ID/Location/Specialty/Instant Approval/Retail), and **client-side pagination** (First/Prev/1‑N/Next/Last). Real data is shown where the rewrite's model has it (specialty, retail); **name / Program ID / location / hospital image / instant-approval are placeholders** pending a later backend slice (the model doesn't carry them yet — Figma is ~90% and the live app needs fields the normalized rewrite dropped). Full **CRUD preserved**: Add button, row-click → edit modal (now with a Delete action), confirm-delete. New reusable `Tabs` + `Pagination` components (with tests). 127 frontend tests (6 new), coverage 90% stmts / 80% branch. Brand pink kept at our agreed **#FF4874** (live app uses #FF0057 — one-token switch if preferred). **Deployed to DEV for owner sign-off before cloning the remaining screens.** |
| 2026-06-19 08:40 | [#36](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/36) | DEV | **Figma design system — foundation pass** (owner decision 2026-06-19: Figma is the visual target; this first pass adopts the Figma *design system* — fonts, colours, button/input styling — while **keeping every screen's current layout**, deferring per-frame layout rebuilds to after go-live). Self-hosts the Figma type faces **PT Serif** (headings) + **PT Sans** (body/UI), latin subset only, 400/700 weights, bundled same-origin to satisfy the strict CSP (`font-src 'self'` — no external Google Fonts). Token layer in `styles.css` moves to the Figma palette (neutrals → black `#212529` / body `#565E67` / gray `#B5BDC6`; brand stays `#FF4874`; adds secondary `#FFC700`, accent-blue `#5AA6FF`, link `#0057FF` for later screens) and the Figma button spec (PT Sans **Bold**, **7px** radius, brand fill with **dark `#212529` text** — the higher-contrast pairing, since white-on-`#FF4874` fails WCAG; sizing kept compact to match the current admin layout rather than Figma's larger default). Global serif headings via family swap only — heading **sizes stay tied to each screen's existing rules so no layout shifts**; body line-height trimmed 1.75→1.5 so dense tables keep their height. Functional coverage unchanged (no fields/forms altered — Figma is ~90% so per-form field-completeness checks belong to the later per-screen rollout). 121 frontend tests green (>90% cov), build clean. **Deployed to DEV for owner sign-off before the system is rolled across the remaining screens.** |
| 2026-06-18 16:30 | [#35](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/35) | DEV | **P3 — admin refunds** (Cancelled/Completed → Refunded). **`POST /api/rotations/{id}/refund`** (AdminOnly) refunds every captured payment through the gateway and moves the booking to **Refunded** — only from a refundable state and only with a captured payment (a repeat is a 409). **Concurrency-safe:** an atomic **conditional-claim** UPDATE (row-locked, inside a transaction) serialises two simultaneous refunds so the money-out runs **exactly once**, rather than trusting provider-side idempotency as the only guard — the same DB-side stance the deposit double-charge guard takes. A direct edit to Refunded via the admin **PUT is blocked** (refunding moves money — it must go through the refund action). Gateway gains `RefundAsync` (fake: deterministic + idempotent per key); `Payment.ProviderRefundId` + `AddPaymentRefund` migration. Admin rotations UI: a **Refund** action on Cancelled/Completed rows → confirm → refund; Refunded removed from the edit-form transitions. 8 refund integration tests (incl. concurrent-refund-exactly-once + Completed-path) + 1 gateway unit test + 1 authz-matrix row; admin-UI tests (refund flow, hidden on non-refundable/already-refunded, error banner, form omits Refunded). 121 frontend (>90% cov), 68 API unit. Two-agent adversarial review + a focused concurrency re-review that verified the claim/transaction fix correct. |
| 2026-06-18 11:30 | [#32](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/32) | develop only | **Vendor TEST-sandbox provisioning guide** (`Docs/Vendor_Sandboxes.md`) — owner-action checklists for the four isolated test sandboxes (Stripe/SendGrid/Twilio/Innodata), each value mapped to its Key Vault secret. **Docs-only — no DEV deploy** (docs are path-excluded from `deploy-dev`). _(Backfilled into the log 2026-06-18 — the row was missed when the docs-only PR merged.)_ |
| 2026-06-18 15:00 | [#34](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/34) | DEV | **P3 — student self-booking** (completes the find→book→pay journey). **`POST /api/customer/rotations`** (CustomerOnly) lets a signed-in student book a rotation themselves: the student is **server-resolved from the CIAM oid** (no `StudentId` in the payload — can't book for anyone else), the program/duration (≥ program min, ≤ 520 weeks)/non-past start date are validated, and the booking is created **Pending** (a student can't self-approve — approval follows the deposit). The portal **program-detail page** gains a booking panel: pick a start date + weeks, see the **server-computed quote** live (total / deposit / outstanding — pricing stays server-authoritative, the legacy client+server duplication stays gone), **Book** → lands on *My rotations* to pay the deposit. 9 booking integration tests (incl. a DB-fixture **ownership** assertion that the row is snapshotted to the caller, and a Preceptor-customer→400 case) + 1 authz-matrix row; ProgramDetailPage component tests (re-quote-on-duration-change, invalid-duration gating, book→navigate, error→no-navigate). 116 frontend (>90% cov), 67 API unit. Two-agent adversarial review (backend auth/identity/validation, frontend state/quote-race) clean. Deferred (acknowledged, not bugs): per-program capacity + duplicate-booking guards, and auto-provisioning a Student record for a fresh CIAM signup (booking currently needs an admin-linked profile). |
| 2026-06-18 13:30 | [#33](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/33) | DEV | **P3 — student deposit checkout UI** (makes the money path usable in the browser). On the portal's *My rotations*, a **Pending** rotation now shows **Pay deposit** → a dialog opens the deposit intent, shows the deposit/total/outstanding breakdown, and completes payment; on success the booking flips to **Approved** (the tracker query is invalidated). Since DEV has no real Stripe.js, fulfilment is completed by a new **DEV-only `POST /api/dev/payments/{id}/simulate`** — the Stripe-CLI analog, CustomerOnly + ownership-checked, gated on **non-Production** so it exists on DEV (`Development`) and the integration-test host (`Testing`) but **never** on PREPROD/PROD (both `Production`); a real provider webhook drives fulfilment there. Webhook + simulate now share **one** `PaymentFulfillment` money path (extracted; behaviour byte-identical). The real Stripe **Elements** card flow is gated behind the publishable key (empty today → "test mode") and lands as its own slice when the Stripe sandbox key arrives (see `Docs/Vendor_Sandboxes.md`). 4 simulate integration tests + 1 authz-matrix row; PaymentModal + My-rotations component tests (112 frontend, coverage >90%). Reviewed via a two-agent adversarial pass (backend gating/fulfilment, frontend state/cache) — env gate proven, fulfilment refactor verified identical. |
| 2026-06-18 11:00 | [#31](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/31) | DEV | **P3 — payment lifecycle: deposit checkout + webhook fulfilment** (behind a fake gateway). The full money path, with the payment provider behind an `IPaymentGateway` abstraction so DEV/tests run on a deterministic **fake** (no live keys) and the real Stripe adapter + Key Vault signing secret slot in at PROD cutover (owner is provisioning the Stripe test sandbox). New `Payment` entity (`payments` schema; amount/total/outstanding, status, provider intent id + idempotency key, both unique; `AddPayments` migration) + a `ProcessedWebhookEvent` idempotency ledger. **`POST /api/rotations/{id}/payment-intent`** (CustomerOnly + oid→student→rotation **ownership** check, so a customer can't open a payment for someone else's booking) opens the deposit computed by `PricingService` (only for a **Pending** rotation — a terminal/approved booking is refused 409); a succeeded deposit is final (409), a pending one is re-offered (same intent, no double-charge). A **partial unique index** (`UX_payments_RotationId_active`, one live Pending/Succeeded deposit per rotation) makes two concurrent opens collide on the DB rather than double-charge — the loser falls back to the existing intent. **`POST /api/webhooks/stripe`** is the ONLY fulfilment path (Plan_Student §52 — closes the legacy browser-callback silent-loss gap): anonymous but **signature-verified** (HMAC, constant-time), **idempotent** (per-event ledger + acts only on a Pending payment), body-size-capped, and on success moves the booking Pending→NotStarted ("Approved") via the state machine (a since-cancelled rotation is left alone). 8 fake-gateway unit tests + 15 checkout/webhook integration tests (incl. concurrent-open-one-row, deposit-for-cancelled→409, already-paid→409, preceptor→404, succeeded-webhook-for-cancelled records-but-leaves) + 1 authz-matrix row (the webhook is intentionally anonymous, tested directly). Reviewed via the reviewer roster with a three-agent adversarial pass (concurrency/money, auth/security, EF/data — payments + webhook are risk areas). The student-facing cart/booking UI, promo/credits/unlocks, refunds, and outstanding-balance payment are later slices. |
| 2026-06-18 09:30 | [#30](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/30) | DEV | **P3 kickoff — payments pricing foundation.** First slice of Phase 3 (Money & documents). A pure, fully-tested `PricingService` is now the single source of truth for booking math (the legacy system duplicated these rules client+server): total = retail/week × weeks; **non-open programs take a 10% deposit** (remainder billed as outstanding), **open/instant-approval programs are charged in full**; all amounts rounded to the cent with deposit + outstanding always reconciling to the total. New `IsOpen` flag on `RotationProgram` (`AddProgramIsOpen` migration; the seeded tele-rotation is open so DEV exercises both paths) — settable via the admin program API (the admin-form checkbox + the student-facing quote display land in the pricing-UI slice). New `GET /api/programs/{id}/quote?weeks=N` (MarketplaceViewer — students need pricing to book) returns the server-computed quote; week count validated against the program minimum. 12 PricingService unit tests + 7 quote integration tests + 1 authz-matrix row. Consultation **hourly** pricing (charge by hours, not weeks) is a tracked follow-up. Reviewed via the reviewer roster (`Docs/Review_Process.md`) — pricing is a money risk area. |
| 2026-06-17 13:05 | — (ff promote) | `main` | **Promoted `develop` → `main`** (fast-forward, 31 commits: PR #1 baseline through #28 SPA hardening). `origin/main` now mirrors `origin/develop` at `43bec17`. Source-control promotion only — no environment auto-deploys off `main` (PREPROD/PROD pipelines deferred per stealth §5; only `rplus-deploy-dev` (develop→DEV) exists today). Establishes `main` as the release line carrying the full DEV-verified history. |
| 2026-06-17 12:30 | [#28](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/28) | DEV | **SPA hardening sweep** (slice 4 of 4) — closes `Plan_Admin §5b` items 1–3. (1) **Token re-auth on expiry**: both `api.ts` (staff) and `customerApi.ts` (customer) now catch `InteractionRequiredAuthError` from `acquireTokenSilent` and fall back to `acquireTokenRedirect`, so an expired session / revoked refresh token re-prompts instead of surfacing a raw error banner. (2) **Security headers** in `staticwebapp.config.json`: a CSP scoped for MSAL (`script-src 'self'`, `connect-src`/`frame-src` for the workforce + CIAM + live.com endpoints), `X-Frame-Options: DENY` + `frame-ancestors 'none'`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`. (3) **Modal focus management**: the reusable `Modal` moves focus into the dialog on open, traps Tab/Shift+Tab (wrapping), and restores focus to the trigger on close — benefits every existing and future dialog. 103 frontend tests (6 new), coverage 90%. **Verify on DEV:** redirect sign-in is CSP-independent; if silent-renewal's hidden iframe is ever blocked it degrades to a redirect via item 1 — tighten `connect-src`/`frame-src` only after confirming the live MSAL traffic. Reviewed via the reviewer roster (`Docs/Review_Process.md`). **Completes the 4-slice batch (student My-rotations · status state machine · admin dashboard · hardening).** |
| 2026-06-17 11:30 | [#27](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/27) | DEV | Admin **dashboard hub** (slice 3 of 4). `GET /api/dashboard` (AdminOnly) aggregates server-side in one request: domain totals (students/programs/preceptors/specialties), the rotation **pipeline grouped by status**, and the next 8 **upcoming starts** (today-onward via `TimeProvider`, ordered). New `/admin/dashboard` page — stat cards + a by-status panel + an upcoming-starts table — as the first item in the admin nav. 4 integration tests (totals/pipeline consistency, future-dated upcoming ordering, 403/401) + 1 authz-matrix row; 97 frontend tests (5 new), coverage 91%. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-17 10:30 | [#26](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/26) | DEV | Rotation status **state machine** (slice 2 of 4). A `RotationStatusMachine` defines the legal lifecycle transitions (Pending→NotStarted/Rejected/Cancelled; NotStarted→Active/Cancelled/Abandoned; Active→ToBeEvaluated/Completed/Abandoned/Cancelled; ToBeEvaluated→Completed/Abandoned; Completed→Refunded; Cancelled→Refunded; Rejected/Refunded/Abandoned terminal). PUT now **rejects illegal jumps** (e.g. Completed→Pending) with a 400 against the current status; create may still record any status (captures reality). The detail response exposes `allowedNextStatuses`, and the admin edit form's status dropdown is **limited to the current status + its allowed transitions** (single source of truth — the server enforces the same graph). 25 state-machine unit tests + 3 transition integration tests; 92 frontend tests, coverage 91%. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-17 09:30 | [#25](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/25) | DEV | Student portal — **My rotations** (slice 1 of 4). The signed-in student's rotation tracker: `GET /api/customer/rotations` (CustomerOnly) returns the caller's own rotations, matched to their directory `Student` by **CIAM oid** (through the live record, so it works even if the admin linked the oid after booking). The student-hidden lifecycle states (Cancelled / Refunded / Abandoned / Rejected, per Plan_Student §7) are excluded; `NotStarted` surfaces as "Approved". New `/portal/rotations` page (rotation cards: specialty, type, preceptor, dates, weeks, status badge) under a portal top-bar nav (Find a rotation · My rotations). 5 customer-rotation integration tests + 1 authz-matrix row; 91 frontend tests (3 new), coverage 91%. **Review caught a HIGH IDOR** — `StudentOid` had no uniqueness, so an admin could link one CIAM oid to two students and the portal would cross-leak their rotations; fixed with a **live-rows filtered unique index** on `StudentOid` + a write-time 409 guard (`UniqueStudentOid` migration) + duplicate-oid tests. Reviewed via the reviewer roster (`Docs/Review_Process.md`). First end-to-end customer flow beyond browse. |
| 2026-06-16 17:20 | [#24](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/24) | DEV | Rotation → Student link (slice 16) — connects the two domains shipped today. `Rotation` gains a nullable **`StudentId` FK** to the directory (`AddRotationStudentLink` migration: FK Restrict + index, seed rotation backfilled to the Sam Rivera student). The admin rotation form's three free-text student fields are **replaced by a directory picker**; on write the server resolves the chosen student and **snapshots** their name/email/oid onto the rotation (so display stays self-contained and a soft-deleted student can't break the list — same projection-safety rationale as the program guard). Create/Update requests now take `StudentId` instead of name/email/oid; unknown student → 400. **Deleting a student with booked rotations is now blocked (409)**, mirroring the program-delete guard. Review caught a snapshot-length bug — the composed `FirstName LastName` (each `varchar(100)`) could exceed the rotation's `StudentName varchar(200)`; widened to 256 (`WidenRotationStudentName`). Rotation integration tests reworked for the picker (incl. oid-snapshot, student-delete-guard, and soft-deleted-rotation-makes-student-deletable); 88 frontend tests, coverage 91%. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-16 16:30 | [#22](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/22) | DEV | Students admin directory (slice 15) — the **demand side** of the marketplace as a first-class `members`-schema entity. A `Student` carries the identity core (name/email-unique/phone), an `AcademicStatus` (US Pre-med / MD / DO / Dental / Int'l Med Student / IMG / PA / NP), an optional `VisaStatus`, school/location, the exact legacy lifecycle `Status` (`Registered`/`MemberProfileCompleted`/`MemberActivated`/`TurnedIntoContact`), and an optional `StudentOid` to link a CIAM account. `AddStudents` migration (unique email, status index, 2 seeds incl. "Sam Rivera" = the seeded rotation's student). **StaffOnly reads** (sales/SDR/coordinator work the directory for CRM), **AdminOnly writes** — duplicate-email 409 + restore-on-recreate + create-race handling mirrored from preceptors; status + academic-status filters. New `/admin/students` screen under the Operations nav group (table, status filter, create/edit/delete). 23 student integration tests + 5 authz-matrix rows; 87 frontend tests (10 new), coverage 91% stmts. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-16 15:35 | [#21](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/21) | DEV | Rotations admin domain (slice 14) — first **operations** module: a `Rotation` entity (`operations.rotations`) booking a student (name/email + optional CIAM oid) into a program over a date range, with the **exact legacy lifecycle vocabulary** (`Pending`/`NotStarted`/`Active`/`ToBeEvaluated`/`Completed`/`Cancelled`/`Refunded`/`Abandoned`/`Rejected`; `NotStarted` shown as "Approved"). `AddRotations` migration (FK→program Restrict, status+programId indexes, one seed). AdminOnly CRUD `/api/rotations` (status + programId filters; server-side date/email validation; `Weeks` derived from the range — rounding a partial week **up** since it will feed per-week pricing). Deleting a **program** is now blocked with a 409 while live rotations reference it (the FK is `Restrict`, which only governs hard deletes — without the guard a soft-deleted program would orphan its rotations and 500 the admin list). 19 rotation integration tests + 5 authz-matrix rows. New admin screen `/admin/rotations` (student/specialty/type/preceptor/dates/weeks/status table, status filter, create/edit/delete) under an **Operations** nav group. 88 frontend tests (11 new); coverage 91% stmts. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-16 14:10 | [#20](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/20) | DEV | **Customer portal MVP** (`/portal`) — student-facing catalog: CIAM sign-in, browse programs (filter by specialty/type/price/text), and a customer program-detail view (no honorarium). Resolves the slice-9 dual-MSAL caveat with **per-route MSAL providers** (no shared root provider) — staff console on the workforce instance at `/`, customer portal on the CIAM instance at `/portal` with its **own redirect URI** so the two never contend for an auth-response hash. Shared `apiFetch` low-level layer; customer API uses the CIAM token. 63 frontend tests (11 new); coverage 92% stmts. **⚠️ Customer sign-in needs the CIAM script re-run** to register the new `/portal` redirect URI (see below). Reviewed via the reviewer roster. |
| 2026-06-16 12:15 | [#19](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/19) | DEV | Open marketplace **catalog reads to customers** — new `MarketplaceViewer` policy (any staff or customer role); `GET /api/specialties[/{id}]` and `GET /api/programs[/{id}]` now serve signed-in Students/Preceptors (CIAM tokens) as well as staff, while writes stay AdminOnly and the preceptor **directory** stays StaffOnly. Authz-matrix read rows widened to staff+customer; a positive integration test confirms a Student can browse the program catalog. Prerequisite for the student-facing portal (next slice). Reviewed via the reviewer roster. |
| 2026-06-16 11:50 | [#18](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/18) | DEV | Admin portal UI slice 11 — **Preceptors management** (`/admin/preceptors`): directory table (name, email, specialty, location, status badge) + create/edit/delete on the AdminOnly endpoints. Form covers identity/professional fields (first/last/email + valid-email check, primary-specialty + status dropdowns, optional license #/state/city/state/bio) with zod validation mirroring the server (length caps, required, email); duplicate-email 409 + unknown-specialty 400 surface in the form; edit pre-fills from preceptor detail and re-edits from fresh cache. Preceptors nav enabled — **the marketplace admin trio (specialties/programs/preceptors) is complete**. 52 frontend tests; coverage 93% stmts / 82% branch. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-16 11:10 | [#17](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/17) | DEV | Admin portal UI slice 10 — **Programs management** (`/admin/programs`): list table (specialty, type label, capacity, retail, preceptor) + create/edit/delete on the AdminOnly endpoints. Form has specialty + program-type + optional-preceptor dropdowns and capacity/money fields, with zod validation mirroring the server (capacity caps, money ceiling + cents-only) and server 400s surfaced in the form; edit fetches the full program detail to pre-fill. Programs nav enabled; `Modal` gains a `wide` variant. 37 frontend tests; coverage 92% stmts / 83% branch. Reviewed via the reviewer roster (`Docs/Review_Process.md`). Preceptors admin CRUD is the last marketplace screen (slice 11). |
| 2026-06-16 09:40 | [#16](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/16) | DEV | Admin portal UI slice 9 — **stands up the SPA architecture** (React Router 6 + TanStack Query 5 + React Hook Form/zod) behind a modern admin shell (sidebar/topbar, brand `#FF4874`, tablet-usable, role-gated nav). First management screen: **Specialties CRUD** (list + create/edit/delete on the AdminOnly endpoints, client zod validation mirroring the server, 409-duplicate/400 surfacing, confirm-delete). Adds `public/staticwebapp.config.json` (SPA navigation fallback so deep links survive refresh). 24 frontend tests; coverage 90% stmts / 84% branch. Reviewed via the reviewer roster (`Docs/Review_Process.md`). Programs/Preceptors admin CRUD follow as slices 10/11 on these patterns. |
| 2026-06-16 07:27 | [#15](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/15) | DEV | Marketplace slice 8 — **CIAM customer-auth wiring**: a second JWT-bearer scheme validates External ID (customer) tokens behind an issuer-routing **"Smart"** policy scheme (one pipeline accepts both staff + customer tokens, role policies gate). New `GET /api/customer/me` (CustomerOnly → Student/Preceptor) proves the round-trip; `AzureAdCustomer` config + customer MSAL config (`rplus-web-ext`/CIAM authority) in the SPA. Unit test for the scheme selector, 4 customer round-trip integration tests + an authz-matrix row (staff→403, customers→through), SPA customer-config tests. Runtime sign-in awaits the portal user-flow + admin consent. Reviewed via the reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-15 23:04 | [#14](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/14) | develop only | Fix `infra/ciam/Configure-Ciam.ps1` for the Windows `az.cmd` toolchain — ASCII-only (PS 5.1 parser), read app registrations via `az ad app show --id` (avoids the parenthesised `applications(appId='..')` Graph URI cmd.exe mis-parses), BOM-free `--body` temp file. Operator-run script; no DEV deploy. |
| 2026-06-15 22:51 | [#13](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/13) | develop only | CIAM customer-auth **setup scripts** — `infra/ciam/Configure-Ciam.ps1` (idempotent app-registration config: exposes `access_as_customer` scope, `Student`/`Preceptor` app roles, SPA redirects + delegated permission, ensures service principals) + `README.md` (run order, portal-only steps, output→config map). Operator-run on the CIAM tenant; no DEV deploy (`infra/ciam` path-excluded from `deploy-dev`). |
| 2026-06-15 22:33 | [#12](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/12) | DEV | Marketplace slice 7 — program catalog **search/filter**: `GET /api/programs` gains optional AND-combined filters (`specialtyId`, `programType`, `maxRetailPerWeek`, free-text `q` over specialty name + description via escaped case-insensitive ILIKE, bounded to 100 chars) alongside `preceptorId`. Backend of student-facing search (StaffOnly until CIAM). 13 read/search integration tests. Reviewed via the reviewer roster (`Docs/Review_Process.md`); paging deferred to a tracked follow-up. |
| 2026-06-15 21:23 | [#11](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/11) | develop only | Process docs — `Docs/Review_Process.md` (world-class reviewer-roster playbook) + `CLAUDE.md` §3 review pointer and §6 mandatory same-step post-merge branch cleanup. No DEV deploy (docs are path-excluded from `deploy-dev`). |
| 2026-06-15 21:06 | [#10](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/10) | DEV | Marketplace slice 6 — wire `Program` → `Preceptor`: nullable `PreceptorId` FK (`AddProgramPreceptor` migration assigning seeded programs to seeded preceptors), `PreceptorName` surfaced on summary + `PreceptorId`/`PreceptorName` on detail, AdminOnly create/update validate + set/clear the preceptor (unknown → 400), `GET /api/programs?preceptorId=` filter, SPA shows the offering preceptor; read + admin integration tests extended. Reviewed via the world-class reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-15 20:29 | [#9](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/9) | DEV | Marketplace slice 5 — `Preceptor` directory (first slice): identity/professional core + primary-specialty FK + lifecycle `Status`, `AddPreceptors` migration (unique email, 2 seeds), StaffOnly `GET` + AdminOnly `POST`/`PUT`/`DELETE` (validation, email-uniqueness 409, soft-delete + restore-on-recreate), SPA list; 4 read + 19 admin integration tests, 5 authz-matrix rows, a frontend component test. Reviewed via the world-class reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-15 17:40 | [#7](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/7) | DEV | Marketplace slice 4 — `Program` admin writes: AdminOnly `POST`/`PUT`/`DELETE` on `/api/programs` with strict money/capacity validation (numeric(10,2) ceiling + cents-only pricing, capacity caps, FK-to-existing-specialty), soft-delete via the audit interceptor; 19 integration tests + 3 authz-matrix write rows. First slice run through the new world-class reviewer roster (`Docs/Review_Process.md`). |
| 2026-06-15 16:16 | [#6](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/6) | DEV | Marketplace slice 3 — `Program` catalog: `RotationProgram` entity (Specialty FK, type, capacity, pricing), `AddPrograms` migration seeding 4 programs, `GET /api/programs[/{id}]`, string-enum API serialization, SPA list, integration + authz-matrix tests. |
| 2026-06-15 14:16 | [#5](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/5) | DEV | Marketplace slice 2 — `Specialty` admin writes: AdminOnly `POST`/`PUT`/`DELETE` (validation, duplicate-409, soft-delete + restore-on-recreate, concurrent-create race handling); 8 integration tests + 3 authz-matrix write rows. |
| 2026-06-15 13:39 | [#4](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/4) | DEV | Marketplace slice 1 — `Specialty` reference data: seeded migration (15 specialties), `GET /api/specialties[/{id}]` (StaffOnly), SPA list, integration + authz-matrix tests (matrix improved to authz-only semantics). |
| 2026-06-15 13:09 | [#3](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/3) | DEV | Observability fix — wire the Azure Monitor OpenTelemetry exporter in `ServiceDefaults` so app traces/metrics/logs reach App Insights (the `APPLICATIONINSIGHTS_CONNECTION_STRING` was previously never read; 0 telemetry). |
| 2026-06-15 12:30 | [#2](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/2) | develop only | Working agreement — feature-branch workflow + approval gates + autonomy rules (`CLAUDE.md`) and this deployment log. No DEV deploy (docs are path-excluded from `deploy-dev`). |
| 2026-06-15 12:13 | [#1](https://dev.azure.com/rotationsplus/Rotationsplus/_git/Rotationsplus/pullrequest/1) | DEV | P2 identity spine — EF data foundation (audit + soft-delete), `StaffProfile` provisioning (`/api/me` now DB-backed), authz-matrix harness, and EF migrations applied in the deploy pipeline. Merged as `6311823`; deployed by `rplus-deploy-dev` build 9. |
| 2026-06-15 ~11:00 | — (pre-PR-workflow) | DEV | P1 foundation — api/worker/SPA skeleton live on DEV; staff Entra (workforce) login round-trip verified (`/api/me` returns Admin identity). Deploy build 3. |
